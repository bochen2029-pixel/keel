# KEEL — Build State & Reconstitution Anchor
*updated 2026-06-14*

> **⏸ AUTONOMOUS-RUN STOPPING POINT (2026-06-14, ~86% context):** `HEAD` = latest on `main` (`git -C C:\KEEL log --oneline -1`), **pushed**, tree clean, **92 / 5 green**, clippy clean, seal `db4377b3` unmoved. **11 slices this run** (essence · I3-redaction-audit · genesis-capture + calibration · serve↔embed I5 parity · **I5 accept-direction lived in-binary** · perception change-gate · **svc::memory + persistent-memory proven in-binary** · whisper ears · `hear()` retina · route-reason ASCII). Genome bounded seams now real: spine · three-tier router · **I5 both directions lived** · perception (change-gate + ears) · **persistent memory across runs** · metrics. **To RESUME the autonomous run** (the standing grant + operator-only carve-outs + priority-ordered next moves) → see the final **"AUTONOMOUS RUN: STOPPING POINT"** entry in `_run_state/WORKLOG.md`. Backup: `C:\KEEL_backups\` (zip + bundle).

> **▶ RESUMED 2026-06-14 (this session, fresh ctx):** the prior stopping-point above is the resume anchor (its grant + carve-outs still apply — re-granted). **+6 slices this run: `svc::driver`** (initiative; §23) **+ daemon select-loop** (§8) **+ config-from-`keel.lock`** **+ perception capture core** (no-dep dHash/`FrameGate`/`see()`) **+ ears OS-capture** (`cpal` mic, feature-gated `mic`) **+ eyes OS-capture** (`xcap` screen grab, feature-gated `screen` → the native Qwen vision; **no separate VLM**). **Now 110 / 5 green** (default; +1 ignored live each under `--features mic`/`screen`), clippy clean (default + features), seal `db4377b3` unmoved, **pushed**. **Operator correction internalized (WAKE_UP anti-drift item 11 + always-loaded memory):** perception (eyes = native Qwen vision, ears = Whisper) is **first-class baked-in genome, NOT 'heavy periphery'** — capture feeds are toggleable modules (§3.5), models resolved-not-embedded (§2.9). Next: embedder/`GOLDEN_RECALL` (format-committing → design-review) · privacy rung-3 (last) · the operator-review memory narrative register. See the latest `_run_state/WORKLOG.md` entry.

> **▶▶ AUTONOMY MECHANISM LANDED (2026-06-14):** the build now **self-perpetuates handoff-free** — `_run_state/ROADMAP.md` (the NOW→DONE blueprint + the autonomy contract) · `_run_state/AUTOSTART.md` (the standing directive every session executes) · `tools/keel-autoloop.ps1` (the respawn supervisor) · `_run_state/AUTOSTART_SETUP.md` (the **one-time** operator wiring: SessionStart/PreCompact hooks + the unattended permission policy). After that one setup the operator **never hands off again** — he just reviews `ROADMAP.md` §5 ISSUES (the operator-only queue) whenever he likes.

> **▶▶▶ PERPETUAL RUN ACTIVE (2026-06-14, fresh ctx):** first session under the self-perpetuating mandate (ROADMAP §0 loop + AUTOSTART; **a compaction is a rehydrate event, not a stop**). Reconstituted + verified by artifact (HEAD was `0b93199`, 110/5 green, seal `db4377b3`). **Slice A1 landed** (ROADMAP Phase A): `listen()` + `see_screen()` perception **retina wrappers** (`svc::perception`, canon §12) — capture→gate→organ→`Percept`, each behind its forwarded `mic`/`screen` feature (minimal-core §3.5); no new dep; +2 unit +2 live-ignored. **Now 112 / 5 green** (default; +1 ignored live each under `--features mic`/`screen`), clippy clean (default + both features), seal `db4377b3` unmoved, **pushed**. **Tooling note:** use the **PowerShell tool** (NOT the Bash/git-bash tool) for cargo — git-bash mangles `$LASTEXITCODE` + surfaces cargo's stderr as a false exit-1 (memory `keel-use-powershell-tool-for-cargo`). **A2 (Driver daemon, L5) landed** (114/5; `keel daemon` self-drove 2 ticks — **lived by artifact**, traces `…-0`/`…-2`, $0; the same-shell output-capture hang on cold-start is a Windows tooling artifact, NOT a daemon defect → ISSUE-8). **A6.1 (perpetual memory) landed** per operator steer (2026-06-15): the Ring-3 **narrative register** (model-authored arc, separate from the lossless factual Tape, I5) + `assemble` layering Ring-0→Ring-3→Ring-2 + a real **self-interview** `consolidate()` prompt — all model-free/testable (117/5 green). A6.2 (the model-dependent generation loop + cold-eyes validation) deferred. **Standing rule added (ROADMAP §0.9): TTL everything + pivot when stuck.** Then **B2** (TraceSink distill corpus, secrets scrubbed) · **B4** (`distill-export` → chat training pairs) · **A4** (I3 output rung — mask-all-output, no-SSN I5 stopgap retired) · **E1** (port-readiness golden-coverage doc) · **A3** first pass (embedder organ + cosine recall; **GOLDEN_RECALL fingerprint case GREEN**; ISSUE-1 decided: brute-force, no sqlite-vec) all landed. **126/6 green.** Operator directive: cut meta-overhead, **never ask again**, kill-timer + round-robin real work (model-free first, then model). See the latest WORKLOG entries.

## ⛑ RECONSTITUTION PROTOCOL — read this FIRST after any compaction/resume, before ANY other action
The conversation summary is **lossy**. The committed repo + the canon are the truth. Before resuming work or answering a new turn:
0. **Read `_run_state/WAKE_UP.md` IN FULL, FIRST** — the pre-digested, reconciled onboarding brief (full intent + soul · current ground-truth state · the next slice · the resolved drift · where every artifact lives). Lowest-context path to 100% up-to-speed; everything below corroborates it. If it doesn't fit one read, read `_run_state/WAKE_UP_part1..5.md` in order, or chunk it (`C:\KEEL\chunker`).
   **Then `_run_state/ROADMAP.md`** (the forward plan NOW→DONE + the **autonomy contract §0** = the loop you execute) **+ `_run_state/AUTOSTART.md`** (the standing autonomous directive). **This run is autonomous + self-perpetuating** — `tools/keel-autoloop.ps1` respawns sessions, and the SessionStart hook re-injects AUTOSTART on the `compact` source (auto-rehydrate). **Don't wait for the operator; execute the ROADMAP** (decide-and-document; route around operator-only ISSUES; halt at ~90% + exit for the supervisor to respawn).
1. **Read this file (STATE.md) fully**, then the live-session handoff in order: `_run_state/handoff/forward-arc.md` (the causal arc + the operator-confirmed next move) → `_run_state/handoff/recent-turns.md` (recent turns, reverse-chronological, newest-first). Those are the **narrative register** (per the `perpetual-memory` skill / REEL); the **factual register** is git + the contracts/goldens — trust it on any conflict.
2. **Read the canon** `C:\KEEL\KEEL_ARCHITECTURE.md` **+ the constitution** `C:\KEEL\CLAUDE.md`.
   ✅ **A session started in `C:\KEEL` auto-loads KEEL's `CLAUDE.md` correctly** (confirm the cwd). *(Historical caution: earlier sessions ran from `C:\loom` and mis-loaded Marrow-L1's CLAUDE.md — the single biggest past source of confusion. If you ever see Marrow-L1's constitution, you are in the wrong cwd; the active project is **KEEL at `C:\KEEL`**, and Marrow-L1 is a read-only reference bench, never the constitution.)*
3. **Verify real state, never recall:** `git -C C:\KEEL log --oneline -8` + `git -C C:\KEEL status`; then from **PowerShell** `cargo check -p keel-contracts` (must be green).
4. Confirm goldens frozen: `C:\KEEL\tests\golden\.frozen.json` sha256 `db4377b3c0b3b245…` (KEEL-native, re-stamped 2026-06-14); the freeze-gate test `goldens_match_the_frozen_hash` enforces it (run `cargo test -p keel-contracts`).
5. **Only then** resume from "Next" below. **Trust files over summary; verify by artifact, never by memory.**
6. **Backstop (per `perpetual-memory` skill):** the full session transcript on disk is the source of truth for anything the distillation dropped — `grep` the session `.jsonl` under `C:\Users\user\.claude\projects\C--loom\` for specifics (or open it in `C:\TRANSPORTER\claude_archive_viewer_v4.html` and concept-search). Do **not** re-read it whole.

## Where we are (2026-06-13)
- **KEEL** = the sovereign genome harness. Canon **v0.2 adopted**. **Native Rust core** (ADR #5). Consumed **embedded or over protocol**. L1 personal tool, not a product.
- **Committed `d83d6ac` on `main`; pushed PUBLIC to `github.com/bochen2029-pixel/keel`.**
- **L0 contracts** (`crates/keel-contracts`): the ten joints + types + §18 error taxonomy. **`cargo check` + `cargo clippy` GREEN** on rustc **1.96.0**.
- **L1 kernel spine — slices 1–2 landed** (`crates/keel-kernel`): `manifest` (YAML; parses the real `keel.lock` → typed tiers/router/cost, reuses L0 `Price`/`Effort`/`Capabilities`) · `context` (trace-id + clock + per-task budget; L0 stays clock-free) · `registry` (tier→`Arc<dyn ModelTier>` container the **wiring layer** fills, so the kernel imports no L2 — a deliberate fix over the bench) · `chain` (the middleware onion; I1/I3/I4 ride here and become unbypassable; middleware observes/gates/transforms-request, the **engine owns `Context`** for accumulation). `check` + `clippy` clean, **12 tests green**. Manifest format = YAML behind serde (swappable); `rust-toolchain.toml` pin deferred (charter §5: a global-toolchain touch).
- **L3 invariant middleware — the deterministic trio landed** (`crates/keel-middleware`): `mw::audit` (I1 — structured `AuditEvent` per call behind a pluggable `AuditSink`; fires even on a blocked call) · `mw::privacy` (I3 — rung-1/2 deterministic mask: operator markers + regex/checksums incl. Luhn cards; redacts on egress, passes through locally; the operator's marker list stays operator-authored) · `mw::cost` (I4 — pre-call budget hard-stop gate). All compose on one chain → invariants proven unbypassable end-to-end. **12 middleware tests green**.
- **L2 first real tier landed** (`crates/keel-adapters`): `local_llama` — HTTP→llama-server (OpenAI chat-completions): multimodal `Content`→`image_url`, GBNF/JSON constrained-decode hook, `Effort.thinking`→Qwen `enable_thinking` toggle, $0 local cost via `compute_cost`. Shared `openai` mapping module (deepseek/anthropic reuse it next). **Live-validated** against the running b9627 server (`-- --ignored` green).
- **L5 Stage-0 capstone — a runnable `keel` binary** (`crates/keel`): assembles the spine from `keel.lock` (manifest → registry(+`local_llama`) → chain(audit·privacy·cost)), runs a prompt end-to-end to the live tier, prints the answer, and `mw::audit` writes JSONL to the file ledger `.keelstate/audit.jsonl` (I1/I2 to disk). **Validated live**: `keel "…"` → correct Qwen3.5-9B answer, cost $0, trace + audit on disk. Added `FileAuditSink` + optional `TierCfg.endpoint`. **The canonical Stage-0 outcome is met: resolve substrate · talk to a tier · log every call.** **32 tests green** (the binary is validated by running).
- **L2 cheap-API tier landed** (`keel-adapters::deepseek`): DeepSeek over HTTPS (`/chat/completions` no `/v1`; `thinking`+`reasoning_effort`), reusing the shared `openai` mapping (the factor-out pays off). **Live-validated** on the real API — correct answer, `reasoning_content` parsed, **real cost** computed; the CLI is now `--tier`-aware (local | cheap-API) with the **I3 egress mask on for cloud tiers** (local stays sovereign-safe). The ledger records both tiers' real costs (local $0 · cheap-API $0.000126). Key in `DEEPSEEK_API_KEY` (env), never a file. (`deepseek` later fixed to honor `Effort.thinking` explicitly — v4-pro defaults thinking ON; lean ⇒ `disabled`.)
- **L2 frontier tier landed** (`keel-adapters::anthropic`): Claude **Opus 4.8** via Anthropic's **Messages API** — its own protocol (the thin gateway: `x-api-key` + `anthropic-version`, top-level `system`, content-block responses), NOT openai.rs. Vision + extended-thinking capable (opt-in via `Effort`). **Live-validated**: `keel --tier frontier "…"` → correct Opus answer, real cost ($0.0014 logged). keel.lock's frontier price ($5/$0.50/$25 per MTok) confirmed against the docs. **The three-tier economy is complete** (local $0 · cheap-API · frontier), all through one invariant chain. Key in `ANTHROPIC_API_KEY` (env). `default-run=keel` so the CLI stays simple alongside `keel-serve`. **15 adapter unit + 3 live.**
- **L2 `store::sqlite` landed — the index + first `Spine` (I2)** (`keel-store`): SQLite **baked into the binary** (`bundled`), behind a `Store` seam. Implements the `Spine` contract — durable `checkpoint`/`resume` of run-state (I2), the first impl of that joint (an upserting `runs` table). The file ledger stays the system of record; the index is derived/rebuildable. 3 tests (roundtrip · upsert-latest · miss). cc-rs found MSVC fine (as `ring` did).
- **L4 `keel-services::router` landed — Stage 1's fusion point** (`DifficultyRouter`): the §9 rules policy — perception/sovereign force local (I3) · projected-cost-over-budget BLOCK (I4) · `kind`→tier (scaffolding→local · core-wire→cheap-API) · escalate up the local→cheap→frontier ladder on repeated oracle failure. **Validated against the frozen `GOLDEN_ROUTER`** (all 6 cases) — the I5 conformance discipline. Built as a **swappable `Router` policy** (the trait is the seam): a learned/cooling `SirpRouter` slots in behind it, gated by the same goldens, with its quality-signal required to be re-grounded on a non-model oracle (the SIRP-review caveat). Latency axis flagged as the SIRP enrichment that needs a `Step` field (operator action). 2 tests.
- **L1 substrate resolver — FULL (c1 + c2) landed** (`kernel::lifecycle`): `keel "…"` **discovers** a running server (probe :1234/:11434/:8080) OR **cold-starts** one when none is up (`launch` spawns llama-server from `keel.lock` paths → polls a hand-rolled HTTP `/health` until the model loads, 200=ready vs 503=loading → returns a detached `LlamaServer` handle, reused on the next call). `SUBSTRATE_UNRESOLVED` only if launch fails. Dependency-free (kernel stays tiny). **Live-proven**: killed the server → `keel "…"` cold-started a fresh one (pid 36008, answered `Atlantic`) → a second call reused it (`Mars`). **KEEL is now self-sufficient.** 5 lifecycle tests (incl. a `/health` 200-vs-503 parse).
- **L5 `serve_openai` landed — KEEL over protocol** (`keel-serve` bin + shared `keel` wiring lib): an axum OpenAI-compatible server (`POST /v1/chat/completions` · `/v1/models` · `/health`) that assembles the spine **once** (resolving/launching the substrate) and runs every request through the same registry + invariant chain. The CLI and server now share one `assemble()` — no duplicated wiring. **Live-validated**: POST → correct Qwen answer, with a `keel` extension exposing `tier`+`cost`. Default thinking = lean (content-forward; `think:true` opts into reasoning). **KEEL is consumable both ways the canon promises — embedded (lib/CLI) AND over protocol (the fleet points at `:7070`).**
- **L5 `engine` — the self-driving spine landed** (`keel::Engine`): builds a **multi-tier registry** of every *available* tier (local always — resolved/launched; a cloud tier only when its key is in the env, else **skipped, not fatal**), each behind its **own egress-correct invariant chain** (local pass-through · cloud masked — **I3 is now a per-tier property**, which is *why* the engine gives each tier its own chain instead of one shared chain with a static flag). Per turn it calls `DifficultyRouter::route(step,ctx)` → runs the chosen tier through that tier's chain; honors `BLOCK` (I4→`BudgetExceeded`); falls back **down** the ladder if the routed tier is unplugged (logs the substitution). `keel "…"` and `keel-serve` now **self-drive** — the router picks the tier, not `--tier` (kept as a manual single-tier override via the lighter `assemble`, which skips the needless local cold-start). New CLI flags `--kind core-wire` / `--sovereign` build the routing `Step`; the server reads `kind`/`sovereign`/`think` extensions and lets image parts force local by themselves (I3). **Live-validated end-to-end, embedded + over-protocol**: scaffolding→local ($0) · core-wire→cheap-API (deepseek ~$0.0001) · `--sovereign`→forced local (I3) · `--tier frontier`→Opus ($0.0012) · no-keys→graceful degrade to local. **Fixed in the same slice**: local routes now run **lean** (`effort_for(local).thinking="low"`) — a bare `None` deferred to llama-server's thinking-on default and produced *empty content* maxed at 2048 tok; policy belongs in the router, so the adapter stays an honest `None`=server-default mapper. clippy clean; `cargo test` green (router goldens still pass). **Known debt (engine layer):** this loop lives in **L5** (`keel::Engine`, pragmatic wiring); the canon (§6/§8/§14) puts the cycle in `kernel::engine` at **L1** over *injected* `&dyn Router/Oracle/Memory/Spine/TraceSink` (as the registry holds `dyn ModelTier`). When the real verify-feedback loop lands, move the loop there and shrink L5 to pure injection — flagged so momentum doesn't bury it. **[PAID 2026-06-14: the cycle now lives in `kernel::engine` (L1) over injected joints; `keel::Engine` (L5) is a pure-injection wrapper — see the `kernel::engine` bullet below.]**
- **L4 `svc::verifier` — the externality layer (I5) landed** (`keel-services::verifier`): the keystone that makes KEEL a *trust economy*, not a wrapper — KEEL routes and runs everywhere, but **only here does it verify against an assertion no model authored** (§0/§10). A **pluggable oracle registry** + three model-free oracle kinds covering the frozen `GOLDEN_ORACLE`: `PropertyOracle` (deterministic property/metamorphic, e.g. no-SSN; unknown property **fails-closed**), `GoldenOracle` (the **joint-wrong** detector — self-tests pass + golden violated → `JOINT_WRONG`, the "everything looked green" failure), `SourceOracle` (trace-to-canon → `INSUFFICIENT_SOURCE` → human). `Verifier::verify` folds verdicts (passed = AND, joint_wrong = OR). **`GOLDEN_ORACLE` section now green** (the I5 conformance discipline). 3 tests. This is the `oracle_failures` source the loop will feed back — the escalation ladder is no longer just latent. clippy clean; full suite green.
- **L1 `kernel::engine` — the canonical loop landed; the L5→L1 debt PAID** (`keel-kernel::engine`): the §8 cycle now lives in the **kernel** over *injected* `&dyn Router` / `&dyn Oracle` / `&dyn Spine` (+ optional `&dyn Memory` / `&dyn TraceSink`) — the same dyn-injection the registry uses for `ModelTier`. Per turn: **(assemble) → route → chain → verify → fold-cost → checkpoint → emit.** This **wires I5 into the running binary** (every turn calls `Oracle::verify`; `Verifier` gained `impl Oracle` — the registry-as-composite, a pure **L4** add, **zero contract edit**), **accumulates I4 cost** into `Context` after each chain (the engine owns the `Context`; `mw::cost` stays the pre-call gate, so no double-count), and **activates I2 checkpointing** (each turn's `Trace` → the SQLite `Spine`). `oracle_failures`/`tier_history` feed back onto the `Step` so the **escalation ladder fires across turns** (proven by `oracle_failure_escalates_on_the_next_turn`). `keel::Engine` (L5) shrank to a **pure-injection wrapper** that builds the per-tier egress-correct chains + the swappable services and hands them to the kernel. **+7 engine tests** (cost-fold · checkpoint · emit-on-pass · skip-emit-on-fail · cross-turn escalation · Ring-0 prepend · BLOCK · ladder-fallback) → **58 green / 4 ignored**, clippy clean, **freeze seal unmoved**. Surfaced: the CLI prints a `⚠` only on a real verdict failure; `serve` adds `verdict_passed`/`joint_wrong` to the `keel` extension. **Scope at the engine landing (since hardened — see the Half A+B bullet below):** the default oracle registry was empty, so a plain chat turn verified vacuously; **Half B fixed that** (default-oracle set + resolver + fail-closed/critical guards). `Memory`/`TraceSink` are injected as `None` until `svc::memory` (Stage 2) — `None` ⇒ no Ring-0 assembly, no distill emit (both no-ops today).
- **I5 governance — golden freeze-gate ACTIVE** (`keel-contracts/tests/golden_freeze.rs` + `examples/golden_freeze.rs`): re-hashes the goldens vs `.frozen.json` (canon §7/§10 — "the contract-freeze IS the governance"). **Operator re-stamped KEEL-native 2026-06-14** — the seal is now `db4377b3…` (was the Marrow-Python `63d5ba7c…`; float `1e-06` vs Rust `1e-6` — **same ratified 21 cases, `golden.json` byte-identical**, the last Marrow tie severed). `goldens_match_the_frozen_hash` is **un-ignored and green**: a golden content change now **fails the build → fix the code, never the golden**. **The agent never self-stamps the seal** — the re-stamp was verified by artifact (dry-run hash == disk seal, diff `.frozen.json`-only), never authored (the one non-delegable act, canon §10 / CLAUDE.md §3).
- **Stage 2 (Half A + Half B) — constrained-decode conformance + I5 teeth in the loop** (2026-06-14): **Half A** — `GOLDEN_MODEL_TIER` green (`passes_golden_model_tier`: cost · schema→`json_schema` decode · reasoning replay) + the **`SchemaOracle`** (the Director's "schema-valid Directive or reject" gate; `jsonschema` w/ `default-features=off` → no network/TLS/cmake, no egress; **draft pinned to 2020-12**, not auto-detected; the load-bearing rejection is tested). **Half B** — the **default-oracle set** (baseline no-SSN on every output) + the **`golden_refs`→`GoldenCase` resolver** in `kernel::engine` (over an injected `EngineConfig`), so `verify` now **bites**: an unresolved `golden_ref` **fails-closed** (→ operator); a `critical` step with no applicable oracle is a **config fault** (canon §8/§10); a plain non-critical no-ref turn still **passes silently** (no false alarm). CLI gained `--critical`/`--golden-ref` (L5 only). **Lived end-to-end**: `keel --critical --golden-ref does-not-exist …` → fail-closed `⚠` *and* the failing verdict persisted in the I2 checkpoint (`run_id a9dd5f3c15eb`, `passed=false`); a plain turn → silent pass. **68 green / 3 ignored**, clippy clean, **zero contract/golden edits, freeze seal unmoved**. *(The deeper-I5 gap — an oracle consuming the resolved cases — is **closed** by the named-gate-completion slice below.)*
- **Stage 2 (named-gate completion) — resolved refs assert · #3 un-neutered · ASCII alarm** (2026-06-14): the **`GoldenDispatchOracle`** (L4) **consumes** the resolved `golden_ref` cases and dispatches by **family** (schema→`SchemaOracle`, property→`PropertyOracle`; conformance-only goldens stay **test-time**, assert nothing) — so a **bare `--golden-ref <schema-golden>` now gates** with no cell pre-registration (the Director's gate is usable directly). **#3 un-neutered:** the no-SSN baseline moved to `EngineConfig.baseline` — an **I3 sovereignty stopgap** (always-on, but **excluded from #3**; STOPGAP until the privacy-rung slice re-homes it as an output-side I3 check). A `critical` step now needs a **correctness** assertion (a resolved-and-fired ref, or a domain oracle), so a critical step with no ref — or a ref to a conformance-only golden (resolving ≠ asserting) — **config-faults** even with the baseline present. The **I5 alarm is ASCII** (`!! verify FAILED - …`), in print **and** the persisted `verdict.failures` (checkpoint + ledger). **Lived end-to-end** (4 traces cross-correlated with the I2 checkpoint): plain → silent · `--critical` no-ref → config fault · `--critical --golden-ref <schema>` → schema-reject *via the ref* · `--critical --golden-ref <conformance-only>` → config fault. **74 green / 3 ignored**, clippy clean, **zero contract/golden edits, freeze seal unmoved**. *Minor follow-ups:* `serve` parity for `critical`/`golden_ref`; the route-reason `→` in the CLI footer is a cosmetic non-alarm print.
- **Stage 2 (`metrics`) — the flywheel instrument; a reader, not middleware** (2026-06-14): `SqliteStore::metrics()` — a **derived, off-loop, read-only** rollup over the I2 index (canon §13), surfaced via `keel metrics` (L5). **`escalation_rate`** = turns whose final tier climbed **above the kind's base tier** (scaffolding=local, core-wire=cheap-API; from `kind`+`result.tier`) — the *right* def, so normal core-wire→cheap-API routing is **not** miscounted (the `tier!=local` proxy was wrong); reads ~0 until a multi-turn Driver feeds `oracle_failures` back. **`rework_rate`** = a **proxy**: model/content verify-fails / turns, **excluding wiring faults** (config-fault / unresolved-ref) — the precise canon §19 metric (convincing-wrong that *escapes* the oracles) needs a downstream/human signal and is **deferred**. Plus `by_tier`/`total_cost`/`turns`. **Lived** over the real index: `turns=7 escalation=0.000 rework=0.143 by_tier=local=7` (the earlier 0.571 was wiring-inflated). **Canon reconciled (visible edit):** §21 `mw.metrics` → a **reader**; the §14 `store::sqlite` row notes the rollup — reporting is a reader, a *future* real-time metric-driven gate would be middleware (door left open). **Upsert note:** one row per run is complete today; a multi-turn Driver migrates the source to the append-only traces ledger (`TraceSink`). **77 green / 3 ignored**, clippy clean, **zero contract edits**, freeze seal unmoved, never touches the loop.
- **Stage 2 (I3 honesty) — redactions are now I1-audited** (2026-06-14): closed a real canon §5.1/§17 contract violation — `PrivacyMiddleware` was computing egress-redaction findings and **discarding** them (silently green). It now emits an `AuditEvent{code:"REDACTION", redactions:[rung{N}:{kind}…]}` through the shared `AuditSink` (**labels, never values** — a leak is forensically traceable). Extended the **middleware-level** `AuditEvent` with a `#[serde(default)] redactions` field (not a frozen contract) + a `redaction()` ctor; `build_chain` passes the sink to privacy; **+2 tests** (a redaction emits an I1 event; a local call emits none). **Canon reconciled (visible edit):** §14's `mw::privacy` row — the force-local *gate* is the router's (§5/§9), not the mask; "redactions I1-audited" is now true. **79 green / 3 ignored**, clippy clean, **zero contract/golden edits, freeze seal unmoved.**
- **Stage 2 (serve↔embed I5 parity + ACCEPT direction lived) — the gate now bites BOTH ways in-binary** (2026-06-14): `serve_openai` (`keel-serve`) hardcoded `critical:false`/`golden_refs:[]`/`grammar:None` — over-protocol consumers got routing + the chain but NOT the I5 critical-step guard, golden-ref resolution, or constrained decode (a §15 co-primary gap). Added `critical`/`golden_refs`/`grammar` to the `ChatRequest`, wired into the routing `Step` + `GenerateRequest` (mirrors the CLI's `--critical`/`--golden-ref` + a GBNF/JSON-schema constrained-decode path serve gets first). **+2 tests (the `keel` crate's first).** Then **proved the I5 ACCEPT direction lived in-binary over protocol** (the genesis-flagged "missing half"): POST `critical:true` + `golden_refs:[<schema golden>]` + `grammar:{required:[path]}` → local model emitted schema-valid `{"path":"/etc/hosts"}` → `SchemaOracle` validated it **via the ref** → `verdict_passed:true`, $0 local. **Both I5 directions now lived** (reject was already; accept was test-proven-only). **81 green / 3 ignored**, clippy clean, zero contract/golden edits, freeze seal unmoved.
- **Stage 1 (perception change-gate) — `svc::perception`; `GOLDEN_PERCEPTION` green** (2026-06-14): the model-free cost-control gate that ships WITH the senses (canon §12) — frames deduped by **dHash** Hamming distance (threshold ~4; static screen = GPU-free), audio gated by **VAD** (silence never transcribed). The frozen `GOLDEN_PERCEPTION` family moves from "conformance-ahead-of-runtime" to **green** (`passes_golden_perception`). No new deps; the capture organs (`PerceptionSource` + `see()`/`hear()` retinas over real substrate) ride on top later. **83 green / 3 ignored**, clippy clean, zero contract/golden edits, freeze seal unmoved.
- **Stage 2 (`svc::memory`) — the persistent self (canon §11); the dormant `assemble`/record nodes lit up** (2026-06-14): the highest-risk seam, a **minimal-safe first cut** (decided-and-documented under the autonomy grant). A Tape-backed `FileMemory` (lossless append-only JSONL **factual register**): `record` appends each Trace; `assemble` injects Ring-0 (empty genome soul) + Ring-2 recent working turns read back from the Tape. Wired into the kernel loop (one §11-grounded edit: append the Trace post-checkpoint) + activated in L5. **Working memory now persists ACROSS `keel` invocations via the Tape.** +4 tests. **Deferred (designed, for a reviewed pass, vs `perpetual-memory.md`):** the model-authored narrative register + consolidation generation, Ring-1/Ring-4 (embedder + `GOLDEN_RECALL`), Ring-2-as-conversation, the Tape↔Spine unification. **87 green / 3 ignored**, clippy clean, zero contract/golden edits, freeze seal unmoved.
- **Stage 1 (`whisper` adapter) — the ears (canon §12)** (2026-06-14): a local, sovereign **transcription organ** (NOT a tier; the router never routes here) — audio → text *before* cognition via whisper.cpp `whisper-cli`; raw audio never leaves the box (I3). `transcribe(wav) → text` (args/parsing factored out for unit tests; live transcription `#[ignore]`'d, needs the model + WAV). No new deps. Substrate at `C:\whisper.cpp\`. **89 green / 4 ignored**, clippy clean, zero contract/golden edits, freeze seal unmoved.
- **Stage 1 (`svc::perception::hear()`) — the ears end-to-end (canon §12)** (2026-06-14): the hear-retina composes the VAD change-gate + the whisper organ + a `Percept` — silence → `None` (gated, never transcribed); voiced → transcribe locally+sovereignly → Audio `Percept` (source = capture topology). Added `keel-adapters` as a `keel-services` dep (services compose adapters, §6). +2 unit tests + 1 `#[ignore]` live. **Next:** a `PerceptionSource` capture organ over a real mic (substrate-dependent). **91 green / 5 ignored**, clippy clean, zero contract/golden edits, freeze seal unmoved.
- **Stage 1 (route-reason ASCII)** (2026-06-14): the CLI footer prints the router's `Decision.reason`; ASCII-safed the em-dashes + the kind→tier arrow (`-` / `->`) so it renders on any console/codepage (the same discipline as the I5 alarm). Goldens assert substrings ("privacy"/"perception"/"budget"), not glyphs — unaffected. +1 test (`route_reasons_are_ascii`). **92 green / 5 ignored**, clippy clean, zero contract/golden edits, freeze seal unmoved.
- **Stage 1 (`svc::driver`) — the initiative seam (canon §7 joint #8 / §8 loop step 1)** (2026-06-14, new run): the three default Drivers behind the one frozen `poll() -> Option<Step>` joint — `UserTurnDriver` (FIFO queue of operator turns), `HeartbeatDriver` (perpetual tick; pure clock-injected `tick()` so the cadence is deterministically testable; reads a clock at L4 — the L0 contracts stay clock-free), `WatchDriver` (poll-based watch-on-change over a caller-owned probe token — the watched edge stays at the edge, so the genome takes no dep on the source). Each stamps `Step.source` (canon §7 — which Driver emitted it). Turns KEEL from "responds to user turns" into **a self that acts** (3 of 4 real projects act without a user turn — DAVE outreach / TARS PULSE / The Box SILENCE), and gives the persistent memory + `metrics` a multi-turn signal to exercise. **Proves the §23 seam falsifier** (one joint expresses user work AND a heartbeat AND watch-on-change). **No new dep** (`std::{time,sync,collections}`). **Scope:** the Drivers; the daemon select-loop (poll drivers + run each emitted `Step` through `kernel::engine`) is the deliberate follow-on. +5 tests. **Gate: 97 green / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Stage 1 (daemon select-loop) — `kernel::engine`, canon §8 `select(drivers).poll()`** (2026-06-14, new run): finishes the Driver. `select(drivers, ctx)` polls drivers in **priority order** → first `Some(Step)`; `Engine::tick` runs one Step through the full loop; `Engine::run_until_idle` is the **bounded** wrapper (ticks to idle or `max_ticks`, each turn a **distinct `trace_id`** `{base}-{n}` → its own checkpoint/Tape entry/`metrics` row, cost accumulating in the shared `ctx` — I4); `request_from_step` bridges `Step.content` → the user turn. Makes KEEL **a self that acts** and gives memory + `metrics` the real multi-turn signal. **Scope:** the loop logic, built + tested; the continuously-running daemon (idle=sleep) is a thin L5 follow-on, **not started**. L1 (`Driver` is L0 — layer-correct), **no new dep**. +6 tests. **Gate: 103 green / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Stage 0 (config-from-`keel.lock`) — substrate config is now keel.lock-driven** (2026-06-14, new run): retired the hardcoded launch consts (`LLAMA_EXE`/`MODEL`/`MMPROJ`) + `max_tokens=2048` from `keel/src/lib.rs`. `kernel::manifest` now parses `servers` (llama_cpp `path`/`exe`/`endpoint`, `models_dir`) + `substrate.llm_vision` (`file`/`mmproj_file`) + a tier `max_tokens` (default 2048), with `llama_exe()`/`llm_model_path()`/`llm_mmproj_path()` helpers that join dir+filename (the `id` is normalized, so keel.lock now carries the on-disk `file`/`mmproj_file`). The cold-start reads them and **fails honestly** (`SUBSTRATE_UNRESOLVED`) if unconfigured — never a guessed binary. keel.lock gained `exe`/`file`/`mmproj_file`/`max_tokens` (operator-authorized; **no `sha256:` pin touched** — keel.lock is config, not a frozen surface). **No new dep.** +manifest assertions (real-keel.lock parse · path derivation · absent→None). **Gate: 103 green / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Stage 1 (perception capture — model-free core)** (2026-06-14, new run): real **dHash** from raw pixels (`ChangeGate::dhash`/`dhash_rgba` — luma + average-pool to 9x8 + adjacent-bit compare; uniform→0; guarded on zero-dim/short buffers) + a stateful **`FrameGate`** (dedups vs the last *emitted* frame so slow drift still crosses the bar; first = baseline) + the **`see()` retina** (mirrors `hear()`: dHash-gate → Image `Percept` on change; static → `None`, GPU-free; the frame captions downstream via the cognition protocol — image forces `local`, I3; no tier call in the retina). **Autonomous decision (logged):** avoided the `image` crate — a captured frame is already pixels, so dHash is pure arithmetic; file-decode is periphery, not the core. **No new dep** (`std` + existing `serde_json`). +5 tests (fixed a `manual_checked_div` lint). **Deferred (dep-gated):** the OS-capture `PerceptionSource` over a real screen/mic. **Gate: 108 green / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Stage 1 (ears OS-capture device) — `cpal` microphone + no-dep VAD/WAV** (2026-06-14, new run): the first real **capture device** — `keel-adapters::mic::Microphone` (cpal; default input device → mono `i16`, blocking record, sovereign-local) **behind the opt-in `mic` feature** so the default genome core pulls **no** audio dep (minimal-core thesis). Plus the no-dep core: `ChangeGate::voiced_ms` (energy VAD — samples → voiced ms) + `keel-adapters::write_wav_i16` (16 kHz mono RIFF/WAVE for whisper). **Autonomous dep decision (logged):** chose **ears (cpal) over eyes (screen)** — the screen crates are heavy + pull `image` (`xcap`) or the big `windows` crate (`windows-capture`); `cpal` is lighter, no `image`, and completes the already-built whisper + `hear()` chain. **First new dep: `cpal` 0.17**, feature-gated. +2 no-dep tests (VAD · WAV header); the live mic capture is `#[ignore]`'d (operator-verified, the whisper pattern). **Gate: 110 green / 5 ignored (default); `--features mic` compiles + clippy-clean (+1 ignored live); zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Stage 1 (eyes OS-capture device) — `xcap` screen grab → the native Qwen vision** (2026-06-14, new run): `keel-adapters::screen::ScreenCapture::grab()` (xcap 0.8; primary monitor → raw RGBA8) **behind the opt-in `screen` feature**. Feeds the **already-built** `dHash`/`FrameGate`/`see()` → the **native Qwen3.5-9B vision** (`local_llama` image_url) captions it — **no separate VLM, no captioning organ** (unlike ears, which needed whisper; the eye IS the brain, canon §2.8), so eyes was *smaller* than ears (only the grab). **Reframe (operator correction, logged — WAKE_UP item 11):** the prior "screen crates are heavy / flag first" was a **priority inversion** — perception is first-class genome; `xcap`→`image` is fine **feature-gated** (the default core stays `image`-free; the toggle model §3.5). **2nd new dep: `xcap` 0.8**, feature-gated. The live grab is `#[ignore]`'d (operator-verified, needs a display). **Gate: 110 green / 5 ignored (default); `--features screen` compiles + clippy-clean (+1 ignored live); zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Stage 2 (A1: perception retina wrappers) — `listen()` + `see_screen()` (canon §12)** (2026-06-14, perpetual run): bundled the capture devices + gates + organs into the two afferent retinas, each behind its forwarded feature (minimal-core §3.5). `listen()` (`mic`) = `Microphone::capture` → VAD-gate (`voiced_ms`; silence short-circuits — no WAV, no whisper) → `resample_to_16k` (no-dep linear, pass-through at 16 kHz, honors the `mic.rs` caller-resamples contract) → `write_wav_i16` → `hear()` → Audio `Percept`. `see_screen()` (`screen`) = `ScreenCapture::grab` → the already-tested `see()` (dHash `FrameGate` → Image `Percept` on change). **Sovereign + local** (raw audio/frames never egress, I3); the changed frame rides the cognition protocol to the native Qwen vision downstream. Factored the hardware-free `listen_from_samples` (always-compiled) so the silence-gate is unit-tested without a mic; the voiced mic + the screen-grab live paths are `#[ignore]`'d (operator-verified). Forwarded `mic`/`screen` from `keel-adapters` → `keel-services`. **No new dep** (capture deps ride only the opt-in features). +2 always-compiled tests (resample pass-through/down/up + guards · silence-gated-no-transcribe) + 2 feature-gated live `#[ignore]`'d. **Gate: 112 / 5 green (default), clippy clean (default + `--features mic` + `--features screen`), both feature test-builds compile, secret-scan clean, zero contract/golden edits, seal `db4377b3` unmoved.** Pushed.
- **Stage 2 (A2: the Driver daemon, L5) — `keel daemon`, canon §8** (2026-06-15, perpetual run): the thin L5 wrapper the kernel docstring defers here. `keel daemon [--max-ticks N] [--interval MS] [--watch PATH] [--prompt …]` wires a `HeartbeatDriver` (+ optional file `WatchDriver`) and runs the §8 loop — `tick` (select→run→verify→checkpoint→Tape) → on idle, sleep `--interval` and re-poll. **Bounded by default** (`--max-ticks 1`, terminates); `--max-ticks 0` or `--watch` w/o an explicit bound = perpetual. Exposed `keel::Engine::tick`/`run_until_idle` (passthroughs to the tested kernel loop) + the pure CI-tested helpers `watch_token` (file→change token for the `WatchDriver` probe) + `daemon_perpetual` (the bound decision). **Lived BY ARTIFACT** (the verify-by-artifact discipline, not a hanging live capture): a `--max-ticks 2` run self-drove 2 heartbeat turns end-to-end — distinct traces `28c2fa8d5250-0`/`-2` (attempt 0 ran, 1 idled, 2 ran — the bounded select-loop), local tier, $0, ok:true, each checkpointed (index) + Taped + audited; `keel metrics` counted the turns. **No new dep.** +4 tests (2 keel-lib: watch_token, daemon_perpetual). **Gate: 114 / 5 green, clippy clean, zero contract/golden edits, seal `db4377b3` unmoved.** Pushed. **Deferred → ISSUE-8:** same-shell live output-capture can hang on Windows cold-start (the detached server inherits keel's std-handle pipe; daemon itself exits fine). A gate-green detach patch did NOT resolve it → reverted (honest checkpoint); root cause needs more digging.
- **Stage 2 (A6.1: perpetual memory — the narrative register) — `svc::memory` (canon §11)** (2026-06-15, operator-directed): advanced the canon's highest-risk seam with a **safe, model-free, additive** cut behind the frozen `Memory` trait (no contract change). Added the Ring-3 **narrative register** — `FileMemory::narrative()`/`set_narrative()` over a sibling file of the Tape (`<tape_stem>.narrative.md`) — the model-authored, lossy compressed arc, **kept separate from the lossless factual Tape** (I5: a model may not author its own ground truth; the narrative carries the *arc + intent*, the Tape the *facts*). `assemble` now layers **Ring-0 soul → Ring-3 narrative → Ring-2 recent turns**. `consolidate()` returns a `Step` carrying a real **self-interview / forward-narrative** prompt (per `docs/proposals/perpetual-memory.md`) over the prior narrative + recent turns — the model authors the result when it's routed. **Deferred (A6.2, model-dependent/higher-risk):** wiring the consolidation loop (Driver→engine generate→L5 `set_narrative`), the cold-eyes validation Step (diff narrative vs Tape, I5), a swappable consolidation policy, Ring-1/Ring-4. **+3 tests** (narrative injects into assemble & is separate from the Tape · consolidate prompt carries prior narrative + recent turns + the I5 boundary · narrative path is a distinct per-tape sibling). **Gate: 117 / 5 green, clippy clean, zero contract/golden edits, seal `db4377b3` unmoved.** Pushed.
- **Stage 3 (B2: the flywheel feed) — `svc::trace_sink::FileTraceSink` (canon §8 step 9 / §5 reversibility gate)** (2026-06-15): the first Stage-3 slice. A passed `VerifiedTrace` → an append-only JSONL **distill corpus** (`.keelstate/traces/corpus.jsonl`), with **secrets/PII SCRUBBED before write** — the reversibility gate (a secret fossilized into a LoRA is irreversible; undo = retrain). The scrub reuses the **same `Redactor`** (rungs 1–2) the I3 egress mask uses — **one definition of "secret"**, so the corpus can never carry what egress would have masked (`keel-services` gained a `keel-middleware` path dep; services→middleware is layer-legal + acyclic). Scrubs exactly the (prompt, completion) a distill run trains on: `step.content` + `result.content`/`reasoning_content`; the trace structure survives (still a usable record). Wired into the engine's existing emit-on-pass (L5 `trace_sink: Some(FileTraceSink::new(TRACES_PATH, redactor.clone()))`). **No new external dep.** **+2 tests** (secret/ssn/email scrubbed before write, structure intact · one JSONL line per trace + clean text verbatim). **Gate: 119 / 5 green, clippy clean, zero contract/golden edits, seal `db4377b3` unmoved.** Pushed.
- **Golden cases**: ratified + **FROZEN** (`tests/golden/golden.json` + `.frozen.json`, 21 cases / 6 sections). Language-neutral conformance; **agent read-only**.
- Docs: `CLAUDE.md` (build constitution), `AUTONOMY_CHARTER.md`, `README.md`, `keel.lock`.
- **Reference bench**: Marrow-L1 (Python, green, golden-tested) at `C:\loom\marrow-l1` — diff the Rust core against it + the goldens (the ASTRA-textverse pattern). Don't port its code.
- Substrate (resolved, local): `C:\llama.cpp`, `C:\models` (Qwen3.5-9B + `mmproj-F16`; whisper `large-v3-turbo`; openai privacy-filter), `C:\whisper.cpp`.
- Toolchain: rustc **1.96.0** (a stuck 1.95→1.96 rustup update was completed during setup; `rust-std` re-fetched). **Build from PowerShell/MSVC env, not git-bash.**
- **Op note (API keys):** `DEEPSEEK_API_KEY` / `ANTHROPIC_API_KEY` live at **User scope** (persistent, present). A shell started *before* they were set won't inherit them — the engine then wires **local-only** and skips the cloud tiers (by design, not a bug). To validate cloud routing from such a shell, inject first: `$env:DEEPSEEK_API_KEY = [Environment]::GetEnvironmentVariable('DEEPSEEK_API_KEY','User')` (same for `ANTHROPIC_API_KEY`). A fresh operator terminal inherits them automatically. **Live procs:** `llama-server` :8080, `keel-serve` :7070 (restart it after rebuilding that bin; it needs the keys in its env to wire cloud tiers).

## Next — Stage 0 (the spine). Do NOT build it all at once; contract-first, golden/bench-gated.
- **kernel**: ~~manifest · context · registry · chain~~ (slices 1–2 ✓) → **lifecycle + substrate-resolver** · engine · lock
- **invariant middleware** (`crates/keel-middleware`, L3): ~~cost (I4) · audit (I1) · privacy rungs 1–2 (I3)~~ ✓ — deterministic trio complete; privacy **rung 3** (OpenAI Privacy Filter, a verification pass) is Stage 2 behind `GOLDEN_PRIVACY`
- ~~**a (deepseek)** · **c (resolver)** · **b (serve_openai)** · **frontier (Opus 4.8)** · **store::sqlite (Spine/I2)**~~ ✓.
- **Stage 0 spine is essentially complete** (self-sufficient · three-tier economy · invariant chain · ledger + index · embedded + protocol). **Deferred:** kernel `lock` (substrate-hash verify — a no-op until the operator pins the `sha256: TODO` fields; the golden-freeze check belongs with Stage 2's freeze gate) · kernel `engine` (the canonical loop — better in Stage 1, it ties router + drivers + amplify; the CLI/serve already run the minimal route→chain→result).
- ~~**Stage 1: `DifficultyRouter`** + **wire the engine**~~ ✓ · ~~**`svc::verifier`** (I5 externality — `GOLDEN_ORACLE` green)~~ ✓ · ~~**`kernel::engine` (L1)** — the real loop over injected joints; I5/I4/I2 wired live; L5→L1 debt paid~~ ✓ · ~~**I5 freeze-gate** — operator re-stamped KEEL-native (`db4377b3…`) + un-ignored; `goldens_match_the_frozen_hash` green~~ ✓ · ~~**Half A: constrained-decode conformance** — `GOLDEN_MODEL_TIER` green + the `SchemaOracle` (draft-pinned 2020-12; rejection-tested)~~ ✓ · ~~**Half B: I5 teeth** — default-oracle set + `golden_refs`→`GoldenCase` resolver + the 3 hardenings + `EngineConfig`~~ ✓ · ~~**named-gate completion** — `GoldenDispatchOracle` (resolved refs assert by family) · #3 un-neutered (baseline = I3 stopgap, excluded) · ASCII alarm (print + persisted)~~ ✓ · ~~**`metrics`** — off-loop reader over the I2 index (escalation/rework/by-tier/cost); `keel metrics`; canon §21/§14 reconciled to reader~~ ✓ (all 2026-06-14). **Next (sequenced) → the WORKLOG run entries are the authoritative, priority-ordered next-moves.** LANDED this run: **`svc::driver`**, the **daemon select-loop**, **config-from-`keel.lock`**, the **perception capture core**, and **both OS-capture devices** — ears (`cpal` mic) + eyes (`xcap` screen → the native Qwen vision). **Next:** (1) embedder/reranker + `GOLDEN_RECALL` (`sqlite-vec` + Qwen3-0.6B; the **format-committing primitive — design-review candidate**, propose the fingerprint/index approach for the operator's OK first) · (2) privacy **rung-3** (`ort`/ONNX; **least urgent per the operator — last**); and the **operator-review** memory **narrative register** + consolidation. **Dropped until a real trigger:** `mw::cache` (§22), `kernel::lock` (no-op until `sha256:` pins set), `amplify`-OFF (§8/§23). Also no-dep when wanted: the `listen()`/`see_screen()` retina wrappers (bundle capture→gate→Percept) · re-home the no-SSN baseline into an I3 output rung · the daemon's continuously-running L5 wrapper. **No operator-pending items.**
- **SIRP harvest backlog** (from the review): latency axis (needs a `Step` field — operator-frozen) · Layer-2 semantic-abstraction preprocessor (a future I3 "text retina": abstract-for-reasoning-and-privacy then re-inflate) · cooling = the Stage-3 flywheel. `SirpRouter` = a future swappable `Router` policy, only once its quality-signal rests on a non-model oracle (I5).
- **Memory proposal captured** → `docs/proposals/perpetual-memory.md` (operator's by-hand continuity techniques → KEEL Memory-organ enrichments: dialogic-handshake / pre-compaction self-interview · reverse-recency load + forward narrative · self-curated **narrative** register + lossless **factual** Tape + cold-eyes validation · continuous append = capture sanctity). Non-binding; revisit at the **`svc::memory` slice (Stage 2)**. Two-layer: a `/checkpoint`+`/rehydrate` **skill now** (harness-agnostic) + Memory-organ enrichments later.
- **one local adapter** (HTTP → llama-server)
- **invariant middleware**: audit (I1) · privacy rungs 1–2 deterministic (I3) · cost (I4)
- **file ledger** (I2) + **SQLite store** (the index)
- **CLI** + **`serve_openai`** (OpenAI-compatible egress)
- Outcome: a binary that resolves the substrate, talks to a tier, logs every call, consumable embedded or over protocol. **Falsifier: > ~2 weeks → rethink the native-core thesis.**
- **The canon's first real cell = SEXTANT** (canon §17/§21) — the overnight job-search conductor, the most agentic of the seven; *done* = its Conductor/Router/Gate/Canon/State all come **from** KEEL unchanged, only job-domain periphery written. If a cell ever forces a kernel/contract edit, KEEL's boundary is wrong — fix KEEL first.
- **The first external *dogfood consumer* (NOT a cell) = the Backrooms game's Director** — operator-stated **live in-session** (2026-06-13). *Status correction: an earlier version of this line called the Director "KEEL's first cell" — it is **not**. A **cell** is genome + periphery grown on KEEL (that is SEXTANT); the Director is an **independent app** that merely **consumes** KEEL as a localhost sidecar over `serve_openai`. (The provenance was also once mis-cited as "discussed pre-compaction" — a **confabulation** the warm pre-compaction fork disproved; it was operator-stated live, and is logged as the kind of fabricated citation the memory discipline exists to catch.)* Game = **`C:\backrooms`** (C++ / D3D12+DXR raytraced Backrooms sim; KEEL and it are **independent** beyond the service boundary — KEEL depends on no game, and the game treats KEEL as a swappable sidecar). The **Director** (`C:\backrooms\director`, an L4 module; **M0 stub today**, LLM host = milestone **M11**, not started) is its pacing brain — verified against `director/MODULE.md`: **WandererSummary JSON → schema-validated Directive JSON; invalid rejected + logged, never partially applied**; hosts llama.cpp on its own thread; `--no-director` killswitch (INV-6); determinism preserved by the Event Log (replays consume the logged directive, not the model). It will **consume KEEL as a localhost sidecar** over `serve_openai` (:7070), pinned **local** tier + single-shot + `sovereign`/`scaffolding` (no escalation, best-of-N OFF, p95 < 5 s) — which lets backrooms **drop** its planned llama.cpp/vcpkg dependency (KEEL owns the substrate). **Priority-setting convergence:** the Director's "schema-valid Directive or reject" *is* a non-model oracle (I5) + constrained decode — so KEEL's next internal slice (verifier + constrained-decode conformance) builds the Director's literal gate. Backrooms M11 can begin consuming KEEL's **Stage-0** service **now** (it does not block on the verifier). **NightClerk / NightScribe** = alternative/earlier controlled-experiment cells (known shape, clean boundary signal).

## Disciplines (don't drift)
- Contracts + goldens are **frozen** (agent read-only). The contract-freeze IS the governance.
- Layer rule: `contracts ← kernel ← {adapters, middleware} ← services ← apps`.
- Five invariants + reversibility gate on every call; **memory-safety = I5 on the source**.
- Size to the **flywheel base case** (worth it even if `escalation_rate` stays flat).
- **Do not mutate the global Rust toolchain** without asking. No sovereign/vector egress. No secret into a LoRA.
- Build sessions follow `CLAUDE.md` §Session-protocol; end with layer-check → budget → golden-freeze (verify unchanged) → `cargo test` green → one commit, one-line intent.
