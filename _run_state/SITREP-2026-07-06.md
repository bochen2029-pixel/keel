# SITREP — 2026-07-06 (wake-up + bearings; no actions taken)

**Author:** Claude (Fable 5, `claude-fable-5`), fresh session in `C:\KEEL`, operator present.
**Directive executed:** "awaken, get situated, read the core docs in full, report back." This file is the durable log of that sitrep. **The only write this session is this file.** No cleanup, no git operation, no commit, no state mutation was performed — the working-tree anomaly below is charter-gated (restore/checkout on uncommitted work is prohibited without the operator's word), so it is *documented here, not touched*.
**Register honesty:** items marked **[lived]** I executed/observed this session; items marked **[reconstructed]** I read from the record (git, docs, reviews). Per house discipline, trust artifact over any narrative — including this one.

---

## 1 · What was read (full reconstitution chain)

In full, this session: `CLAUDE.md` · `KEEL_ARCHITECTURE.md` (canon v0.2.0, all 23 §) · `README.md` · `AUTONOMY_CHARTER.md` · `_run_state/STATE.md` · `_run_state/WAKE_UP_part1..5.md` · `_run_state/ROADMAP.md` (on-disk = the reverted version; HEAD's version recovered via `git diff`) · `_run_state/AUTOSTART.md` · `_run_state/OPERATOR_DIRECTIVE.md` · `_run_state/handoff/forward-arc.md` · `_run_state/handoff/recent-turns.md` · `_brainstorm/GLM/README.md` · `_brainstorm/KEEL_HINDSIGHT_REVIEW_by_fable5.md` · the full uncommitted diff of every modified tracked file.

## 2 · Verified state, by artifact [lived]

| Check | Result |
|---|---|
| `git log --oneline -1` | HEAD = `6ac319d` "fix(qc): address confirmed audit findings…" (2026-06-15) |
| Branch vs remote | `main`, **up to date with `origin/main`** (pushed; public repo) |
| Golden freeze-gate | `cargo test -p keel-contracts` → `goldens_match_the_frozen_hash ... ok` — **seal `db4377b3` unmoved**; contracts crate green |
| Workspace gate | `cargo check`/`cargo test` on the working tree → **FAILS, exit 101** — `error[E0432]: unresolved import 'crate::recall'` at `crates/keel-services/src/memory.rs:37` |
| Working tree | **dirty**: 8 tracked files modified/deleted (uncommitted) + untracked `_brainstorm/` + untracked junk file `nul` |

Timeline: last commit **2026-06-15** → working-tree overwrite **2026-06-17 09:36** → GLM recon **2026-06-17 13:43** → Fable-5 canon review **2026-07-01** → today **2026-07-06**. The repo has been dormant, with a broken tree, for ~19 days.

## 3 · THE FINDING — the working tree is an incomplete, uncommitted revert (broken since 2026-06-17)

All eight tracked changes share a uniform mtime of **2026-06-17 09:36:31–32** (every `_run_state` file was rewritten in that same second, including ones whose content matched HEAD and therefore show no diff). The tree does not correspond to any commit in history.

**The eight tracked changes (all are rollbacks to older-than-HEAD content):**
1. `crates/keel-adapters/src/embed.rs` — **DELETED** (the A3 embedder organ).
2. `crates/keel-adapters/src/lib.rs` — `pub mod embed;` + `pub use embed::Embedder;` removed.
3. `crates/keel-services/src/lib.rs` — `pub mod recall;` + recall re-exports removed. **`recall.rs` itself is still on disk** (tracked, unmodified) and `memory.rs`'s Ring-4 wiring still does `use crate::recall::…` → **this is what breaks the build** (E0432).
4. `docs/conformance-coverage.md` — **DELETED** (the E1 port-readiness deliverable).
5. `_run_state/STATE.md` — the ▶▶▶ banner rolled back: loses the whole-flywheel-LIVED record, the 48-agent QC pass (GREEN verdict + fixes, 130/6), and the "NEXT: D1" cursor; also loses the ⚡ OPERATOR_DIRECTIVE pointer in the ⛑ protocol.
6. `_run_state/WORKLOG.md` — last 19 lines deleted: whole-flywheel-LIVED · daemon `--consolidate-every` · D1-SCOPED · the 2026-06-15 session checkpoint · the overnight-continuation section (cold-eyes built + LIVED-caught-real-drift, B3/C1/C2 status, the QC pass).
7. `_run_state/ROADMAP.md` — rolled back: A3 `[~] first-pass-done` → `[G]` gated; A6.2 progress erased; B3/C4/C5 prelim decisions erased (rework 5.6% PASS · ~78% cost-favorable); D1 `[~] SCOPED` → `[ ]`; E1 `[x]` → `[ ]`; ISSUE-1 resolution, the ISSUE-8 proven workaround, and **ISSUE-10 entirely** erased.
8. `_run_state/WAKE_UP_part4.md` — rolled back to the pre-engine (2026-06-13-era) invariant scorecard ("I5 not in the loop", "freeze-gate dormant") — contradicted by HEAD and by the passing freeze-gate.

**Forensic notes:**
- **Zero novel content.** Every hunk was inspected [lived]: each is a regression to text that exists verbatim in git history. Nothing in the uncommitted changes is new work. Cleaning the tree to HEAD would lose nothing.
- **Internally inconsistent** — not a checkout of any single commit: the surviving WORKLOG portion still records A3/Ring-4 as landed while the A3 code itself is deleted, and `recall.rs` survives while its module declaration doesn't. Consistent with a partial mirror/restore from an older snapshot (mechanism unknown; possibly related to the parallel-agent fleet working shared directories — cf. Fable-5 review §4.2, which notes fleet concurrency is governed only by verbal convention).
- **Corroboration:** the GLM recon of 2026-06-17 (same day, hours later) independently diagnosed the identical state: *"the working tree does NOT match HEAD and does NOT compile — an uncommitted, incomplete revert on top of `6ac319d` … Trust committed HEAD as the real state."* So this is a stale accident that has sat since June 17 — not fresh in-flight work.
- **Backups exist** at `C:\KEEL_backups\`: `KEEL_20260614-154740` (bundle+zip) · `KEEL_20260615-022418` (bundle+zip) · `KEEL_20260615-025310` (bundle+zip). Plus `origin/main` holds everything through `6ac319d`.
- **Stray file:** `C:\KEEL\nul` (untracked junk; classic Windows artifact of a `> nul` redirect in the wrong shell; needs `\\?\`-prefixed deletion).

**Recommended remedy (NOT executed — awaiting operator word):** `git stash push` (fully reversible; preserves the broken state in the stash for forensics) *or* `git restore` of the 8 paths — either returns the tree to green `6ac319d`; then delete `nul`; then re-run the full gate to confirm 130/6 green + clippy clean. Undo cost of the stash route: one sentence — `git stash pop` restores the broken state exactly.

## 4 · `_brainstorm/` — untracked external reviews on the operator's desk

| Artifact | Date | What it is |
|---|---|---|
| `_brainstorm/GLM/` (README + `00_BIG_PICTURE` + 6 agent reports) | 2026-06-17 | Read-only 6-agent reconnaissance of the whole repo. Headline: HEAD `6ac319d` GREEN (~130/6, QC GREEN, seal green); working tree broken (as §3); ISSUE-10 open; next = D1. Matches this session's independent findings. |
| `_brainstorm/KEEL_HINDSIGHT_REVIEW_by_fable5.md` | 2026-07-01 | **Advisory, non-canonical** canon review by a Fable-5 instance (canon-only; did not read the code). Thesis: "KEEL is not the self; KEEL is the **body** / guarantee layer — the self is the state." Proposes a **v0.3.0 candidate delta** (~9 items): `Arbiter` seam + salience `Candidate` · `Expectation` records + calibration ledger + earned trust · provenance classes `D/R/H/M` on Percept/Trace/memory · a `Vitals` surface · reflex-store rung 0 + operator-as-tier rung ∞ with typed traps · fleet lease/registry convention · `assemble` coverage manifest · soul-manifest + boot drill in `lock`/`lifecycle` · generalized monoid rollups. Each carries its own falsifier; §7 preserves the refusal discipline; §6 flows three KEEL ideas *backward* into the newer specs. **Everything requires operator ratification; nothing governed was touched.** |

**Durability risk:** both reviews are **untracked** — they exist only on this disk. If wanted durable, they need a commit (or at least a backup copy).

## 5 · Where KEEL actually stands (per HEAD `6ac319d` + git) [reconstructed, artifact-anchored]

- **Done and validated live:** the buildable genome surface — three-tier economy (local $0 · DeepSeek · Opus 4.8) through per-tier egress-correct invariant chains · `kernel::engine` (L1) closing the §8 loop (route → assemble → chain → verify → checkpoint → Tape → emit) · I5 with teeth both directions (reject + accept, lived in-binary, embedded **and** over protocol) · `DifficultyRouter` (GOLDEN_ROUTER ✓) · verifier + JOINT_WRONG + SchemaOracle + GoldenDispatchOracle (GOLDEN_ORACLE, GOLDEN_MODEL_TIER ✓) · perception (dHash/VAD change-gates ✓ GOLDEN_PERCEPTION; `cpal` mic + `xcap` screen feature-gated; whisper ears; native Qwen eyes; `listen()`/`see_screen()` retinas) · memory (lossless Tape + Ring-0/2/3/4 · consolidate LIVED sovereign-local $0 · **cold-eyes LIVED and caught real narrative drift** — the I5 memory capstone) · daemon (`keel daemon`, `--consolidate-every`) · metrics reader · B2 scrubbed flywheel corpus + B4 `distill-export` (whole flywheel LIVED on real turns).
- **Gate at HEAD:** 130 tests green / 6 ignored · clippy clean (default + `mic` + `screen`) · freeze seal `db4377b3` active + green.
- **QC:** 48-agent ultracode audit 2026-06-15 → **GREEN** (0 critical/high; 17 confirmed med/low/info; confirmed mediums fixed in `6ac319d`) → `docs/AUDIT-2026-06-15.md`, `docs/RUN-2026-06-15.md`, `docs/PROJECT-STATE.md`.
- **Prelim falsifiers (small N, recorded):** C4 rework 5.6% < 10% **PASS** · C5 routing ~78% cheaper than cheap-API-everything **KEEL-favorable** · B3 escalation_rate 0.000 over 18 turns (base case; trend needs the out-of-band flywheel).
- **The queue:** **D1** — build the first cell (NightScribe on KEEL; scoped 2026-06-15; C#-app → KEEL over `serve_openai`; the next major effort) · **C1/C2** recall-uplift benchmarks — **blocked by ISSUE-10: the Qwen3-Embedding-0.6B GGUF is absent from `C:\models`** (only the reranker GGUF is present; operator download unblocks C1/C2/A3-live/Ring-4-live) · **A5** privacy rung-3 (`ort`/ONNX — operator's stated LAST) · **E2** DONE review. (E1 is done at HEAD — `docs/conformance-coverage.md` — one of the files the June-17 revert deletes.)

## 6 · Decisions pending the operator (none taken by me)

1. **Tree cleanup** — authorize stash-or-restore of the 8 reverted paths + `nul` deletion (§3). Blocking everything downstream (the tree doesn't compile as-is).
2. **`_brainstorm/` durability** — commit the two reviews (or direct otherwise).
3. **ISSUE-10** — download Qwen3-Embedding-0.6B GGUF to `C:\models` (unblocks C1/C2/A3-live/Ring-4-live).
4. **Ruling on the Fable-5 v0.3.0 candidates** — ratify / reject / defer (each delta has a falsifier attached; contracts + goldens remain frozen regardless).
5. **Resume mode** — supervised session-by-session, or re-grant the autonomous ROADMAP loop (AUTOSTART + OPERATOR_DIRECTIVE of 2026-06-15 are on record; WAKE_UP part-4 §4.3.4 notes autonomy is otherwise SUPERVISED-by-default).

---

*Sitrep complete. Committed repo healthy at `6ac319d` (pushed, gated, sealed); working tree broken by a stale June-17 partial revert containing zero novel work; two external reviews await ratification; four operator decisions queued. Nothing was mutated this session except writing this file. — Fable 5, 2026-07-06.*
