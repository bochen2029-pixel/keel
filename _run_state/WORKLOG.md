# KEEL — Worklog (permanent running history)

> Append-only; newest entries at the **bottom**. A brief, durable, chronological trail of what was
> done, when, and the commit it landed in — the continuous thread *across* sessions/instances.
> Complements `STATE.md` (the live per-slice anchor) and the `SESSION-ACCOUNT-*.md` narratives.
> **Git is the authoritative diff; this is the human-readable trail.** Trust files + git over any
> summary, including this one. Convention per entry: *what — commit `<hash>` [pushed|local] — result*.

---

## 2026-06-14 — reconciliation · intent capture · I3 honesty  (Claude Opus 4.8, 1M ctx)

- **Onboarding / reconciliation (read-only).** Read WAKE_UP (all parts), `SESSION-ACCOUNT-2026-06-14`, the canon, `CLAUDE.md`, `README`, `Cargo.lock`, `STATE.md` in full. Fanned out subagent workflows to (a) read the entire Rust codebase and (b) absorb the 2.17M-token prior-session Tape via the chunker (35 chunks → 7 acts → 1 synthesis) — got a consolidated code-reality picture + the full session arc without burning my own context. No repo changes.
- **Essence / intent capture.** Captured KEEL's spirit & intent prominently so no future session/instance re-explains it: one frozen core → three destinies (embeddable AI bundle / sovereign personal harness / org-orchestration kernel); scale-invariance by toggled modules; *the .NET of my AI apps*; the concrete default primitives (Qwen3.5-9B native vision · Whisper · OpenAI-privacy-filter+regex · DeepSeek-V4-Pro · Opus-4.8 · Qwen3-0.6B · SQLite · MCP); privacy as forward-design; Rust-now / C-C++-port-a-designed-for-future. Landed in WAKE_UP §3.5 (+§0 pointer), `README` ("Why KEEL exists" + status refreshed off "pre-implementation"), `CLAUDE.md` ("The intent"), canon §1 (visible additive edit), + a cross-session memory file. **commit `56e52de` — pushed.** Docs-only; cargo gate unaffected; staged-diff secret-scan clean.
- **Verify-by-artifact baseline.** Ran `cargo test --workspace` myself: **77 passed / 3 ignored**, `cargo clippy` clean, freeze seal `db4377b3` unmoved. Green confirmed independently (lived, not reconstructed). Read `engine.rs` / `privacy.rs` / `audit.rs` / `keel/src/lib.rs` to ground the next slice in the real source.
- **Slice — I3 honesty: redactions are now I1-audited** (closes a real canon §5.1/§17 contract violation). `PrivacyMiddleware` was computing egress-redaction findings and **discarding** them (silently green). It now emits an `AuditEvent { code:"REDACTION", redactions:[rung{N}:{kind}…] }` through the shared `AuditSink` — **labels, never the values** — so a mask (and any miss) is forensically traceable. Extended the middleware-level `AuditEvent` with a `#[serde(default)] redactions` field (NOT a frozen contract) + a `redaction()` ctor; `build_chain` (L5) now passes the sink to privacy; **+2 tests** (a redaction emits an I1 event; a local/non-egress call emits none). Reconciled canon §14's `mw::privacy` row (the force-local *gate* is the router's per §5/§9, not the mask; "redactions I1-audited" is now true). **Gate: 79 passed / 3 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Landed in commit `e47df22` — pushed.

---

## 2026-06-14 — autonomy run authorized: backup + genesis-arc capture  (Claude Opus 4.8, Fast)

Operator granted an autonomous build run: proceed far/fast without asking (interrupt only for *real blockers*), decide-and-document on his behalf, cloud spend authorized (sensibly within ~$20 DeepSeek / ~$25 Anthropic balances; local Qwen free 24/7), push every clean gated slice. **Hard rule: HALT + alert at 90% context / any sign of compaction — operator does handoffs manually.** Operator-only acts stay non-self-authorized: editing a frozen contract, ratifying/re-stamping a golden or the seal, mutating the global Rust toolchain.

- **Full backup taken** (worst-case rollback point, outside the repo): `C:\KEEL_backups\KEEL_20260614-154848.zip` (12.8 MB — repo minus `target/`+`.keelstate/`; **contains `_memories/` keys → keep local, never upload**) + `KEEL_20260614-154740.bundle` (full git history). HEAD at backup = `e47df22`.
- **Genesis-arc capture (to complete the intent picture before building).** Fanned out 56 subagents over the 3 pre-history transcripts (Marrow/Loom genesis 314k tok · the fuller "abc" export 414k tok · early-KEEL 166k tok ≈ 894k tokens / 46 chunks). Reused existing `_transcript_chunks` (file 1) + `_keel2_chunks` (file 3); freshly chunked only the abc file → `_abc_marrow_chunks` (21 chunks). 46 readers → 9 act-narrators → 1 synthesizer, tuned for **intent-evolution + operator decision-calibration**. **Synthesis absorbed** (56 agents, 4.3M subagent tokens): the full arc → `_run_state/GENESIS-ARC.md` (durable, inherited by future instances); the decide-as-Bo calibration → memory `keel-operator-calibration`. The complete arc (genesis → pivot → crystallization → current code) is now held. Confirmed: file 2 (abc) is a strict superset of file 1 (same genesis, +100k tok of higher-res verbatim/forensics). Autonomous genome build begins next.

---

## 2026-06-14 — autonomous genome build  (Claude Opus 4.8, Fast)

- **Verify-by-artifact baseline re-confirmed:** `cargo test --workspace` = **79 / 3** at start of build (after the I3 slice), toolchain green from PowerShell.
- **Slice — serve↔embed I5 parity** (closes the protocol-parity gap, canon §15). `keel-serve` (`serve_openai`) hardcoded `critical:false` / `golden_refs:[]` / `grammar:None` — so over-protocol consumers got routing + the invariant chain but NOT the I5 critical-step guard, golden-ref resolution, or constrained decode (the embed CLI had them; the protocol surface didn't — a §15 co-primary violation). Added `critical` / `golden_refs` / `grammar` to the `ChatRequest` (mirroring the CLI's `--critical`/`--golden-ref`, plus the GBNF/JSON-schema constrained-decode path serve gets first) and wired them into the routing `Step` + `GenerateRequest`. **+2 tests — the `keel` crate's FIRST tests** (the parity fields deserialize; absent fields default cleanly so a plain OpenAI request still works). **Gate: 81 passed / 3 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed. *(The capability to prove the I5 accept-direction in-binary is now wired — live smoke is the natural follow-up.)*
- **Verified — I5 ACCEPT direction lived in-binary, over `serve_openai`** (closes the genesis-flagged "missing half of the gate works"). Launched `keel-serve` :7070 (warm llama-server :8080), POSTed `critical:true` + `golden_refs:["tool call valid against schema under constrained decode"]` + `grammar:{required:[path]}` → the local model emitted schema-valid `{"path":"/etc/hosts"}` (constrained decode) → `GoldenDispatchOracle`→`SchemaOracle` validated it **via the ref** → correctness asserted + passed → critical guard satisfied → **`verdict_passed:true`** returned in the response, $0 local. **Both I5 directions now lived in-binary** (reject was already proven; accept was test-proven-only until now). No code change — a verification milestone (exercises the serve-parity slice end-to-end).
- **Slice — perception change-gate (`svc::perception`, canon §12); `GOLDEN_PERCEPTION` green.** The cost-control gate that ships WITH the senses (without it, perception bankrupts the budget): a model-free `ChangeGate` — frames deduped by **dHash** Hamming distance (threshold ~4; a static screen is GPU-free), audio gated by **VAD** (silence never transcribed). Turns the frozen `GOLDEN_PERCEPTION` family from "described-but-unimplemented" to **conformance-green** (`passes_golden_perception` + a Hamming/gate test). No new deps; the capture organs (`PerceptionSource` + the `see()`/`hear()` retinas over real screen/mic + an image/audio decoder) ride on top in a later substrate-dependent slice. **Gate: 83 passed / 3 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Slice — `svc::memory`: the persistent self (canon §11) — the highest-risk seam, a minimal-safe first cut.** A Tape-backed `FileMemory` (the lossless append-only JSONL **factual register**): `record` appends each Trace; `assemble` injects Ring-0 (empty genome soul — persona is a cell concern) + Ring-2 recent working turns read back from the Tape, folded into the `system` preamble. Wired into the kernel loop (one §11-grounded edit: the engine appends the Trace to the Tape post-checkpoint) + activated in L5 (`memory: Some`). **So working memory now persists ACROSS `keel` invocations via the on-disk Tape** — the dormant `assemble`/record nodes lit up, the I2 deepening. **+4 tests** (record↔assemble recall across calls · bounded window · consolidate Step · engine records each turn). **Decided-and-documented under the autonomy grant** (Memory is the canon's flagged highest-risk seam): the rich/risky parts — model-authored **narrative register** + consolidation generation, Ring-1/Ring-4, Ring-2-as-conversation, the Tape↔Spine unification, capture-sanctity early-record — are **designed-and-deferred** in the module for a reviewed pass (against `perpetual-memory.md`). **Gate: 87 passed / 3 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Verified — persistent memory lived in-binary across two separate `keel` processes.** Cleared the Tape, then: process 1 `keel "remember: my favorite number is 42"` → recorded the turn; process 2 (a fresh invocation — no shared state but the on-disk Tape) `keel "what is my favorite number?"` → **answered `42`**, recalled purely from the Tape that process 1 wrote. Tape = 2 entries, $0 local. The "self that persists" (canon §0/§11) is real — KEEL remembers across runs. No code change — a verification milestone.
- **Slice — `whisper` adapter: the ears (canon §12).** A local, sovereign **transcription organ** (NOT a tier — like the embedder/reranker, the router never routes here): audio → text *before* cognition via whisper.cpp `whisper-cli`. **Sovereign** — raw audio never leaves the box (I3). `transcribe(wav) → text`, with the CLI args + stdout parsing factored out for model-free unit tests; the live transcription is `#[ignore]`'d (needs the model + a WAV, like the tier `live_*` tests). No new deps (`std::process`). Substrate confirmed at `C:\whisper.cpp\` (`whisper-cli.exe`/`whisper-server.exe`; model in `C:\models\`). The `svc::perception` `hear()` retina (VAD-gate + this organ + `Percept`, a cross-crate slice) is next. **Gate: 89 passed / 4 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Slice — `svc::perception::hear()`: the ears end-to-end (canon §12).** The hear-retina now composes the VAD change-gate + the whisper organ + a `Percept`: silence (`voiced_ms==0`) → `None` (gated — the organ is never touched); voiced → transcribe **locally + sovereignly** → an Audio `Percept` (source = capture topology, not a model). Added `keel-adapters` as a `keel-services` dep (services compose adapters, §6 — verified no cycle). +2 unit tests (silence-gated-no-transcribe · Percept assembly) + 1 `#[ignore]` live. The ears path is now complete (gate→whisper→`Percept`); a `PerceptionSource` capture organ over a real mic feeds it (later, substrate-dependent). **Gate: 91 passed / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Slice — route-reason ASCII (the alarm discipline applied to the CLI footer).** The footer prints the router's `Decision.reason`; em-dashes (`—`) and the kind→tier arrow (`→`) were non-ASCII (router.rs reasons + main.rs's substitution note) — ASCII-safed (`-` / `->`) so the footer renders on any console/codepage (the same discipline as the I5 alarm). Goldens unaffected (they assert substrings — "privacy"/"perception"/"budget" — not glyphs). **+1 test** (`route_reasons_are_ascii`). **Gate: 92 passed / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.

---

## 2026-06-14 — AUTONOMOUS RUN: STOPPING POINT  (context ~86%, halted per operator rule)

**EXACT STOPPING POINT for the next session/instance.** `HEAD` = the latest commit on `main` (run `git -C C:\KEEL log --oneline -1`; this run's tip), **pushed** to `github.com/bochen2029-pixel/keel`, tree **clean**, **92 passed / 5 ignored**, clippy clean, freeze seal `db4377b3` unmoved. Full backup (point-in-time rollback): `C:\KEEL_backups\KEEL_20260614-154848.zip` + `KEEL_20260614-154740.bundle`.

**This autonomous run — 11 slices, all gated + pushed:** essence-capture · I3 redaction-audit · genesis-arc + decide-as-Bo calibration · serve↔embed I5 parity · I5 **accept-direction lived in-binary** · perception change-gate (`GOLDEN_PERCEPTION` green) · `svc::memory` + **persistent-memory proven in-binary** · whisper ears · `hear()` retina · route-reason ASCII. **The genome's bounded high-value seams are now real:** spine · three-tier router · **I5 verifier (both directions lived)** · perception (change-gate + ears: whisper + hear) · **memory (persistent across runs)** · metrics — plus the full intent/genesis arc + operator calibration captured durably.

**To RESUME the autonomous run** (operator's standing grant, 2026-06-14): proceed far/fast/wide **without asking** (interrupt only for *real blockers*); **decide-and-document** on the operator's behalf; cloud spend OK (sensibly, within ~$20 DeepSeek / ~$25 Anthropic; local Qwen 24/7 free); **push every clean gated slice**; **HALT + alert at ~90% context** (the operator does handoffs manually — do NOT continue through a forced compaction). **Operator-only acts (never self-authorize, even autonomous):** edit a frozen contract · ratify/change/re-stamp a golden or the freeze seal · mutate the global Rust toolchain.

**Next moves (priority-ordered):** (1) perception **capture** organ — a `PerceptionSource` over a real mic/screen + an image crate for real dHash *(new dep — vet)*. (2) embedder/reranker + `GOLDEN_RECALL` — `sqlite-vec` + Qwen3-0.6B *(new deps — vet)*. (3) privacy **rung-3** — `ort`/ONNX + the OpenAI filter *(new dep — vet)*. (4) **DEFERRED FOR OPERATOR REVIEW:** the memory **narrative register** + consolidation generation (the canon's highest-risk design — flagged for his eyes). (5) no-new-dep bounded: `config-from-keel.lock` · the **Driver seam** · `mw::cache` · `kernel::lock` · amplify-OFF. (6) bigger: the flywheel (Stage 3) · the first cell (SEXTANT / re-home NightClerk).

**Read to resume (lowest-context path):** `WAKE_UP.md` → this `STATE.md` (top banner + per-slice bullets) → this `WORKLOG.md` (the run, chronological) → `GENESIS-ARC.md` → the `keel-operator-calibration` + `keel-essence-intent` memories. **Verify by artifact:** `git log`, `cargo test` (expect 92 / 5), seal `db4377b3` green.
- **`_run_state/INIT_PROMPT.md` refreshed** (the paste-to-start bootstrap prompt for a fresh session): forces full ordered reading (WAKE_UP → STATE → WORKLOG → canon → CLAUDE → AUTONOMY_CHARTER → GENESIS-ARC + the calibration memory → the frozen surfaces) → verify-by-artifact (incl. the **working-tree-integrity** check that catches a stale stitched doc) → restate → propose → STOP for the operator's go. Replaced the stale prior version (which still claimed "I5 unwired / next = kernel::engine / SUPERVISED, no grant"). Operator-gated by default; the autonomous grant is opt-in via the operator's re-grant, carve-outs intact.

---

## 2026-06-14 — autonomous run RESUMED: the Driver seam  (Claude Opus 4.8, 1M ctx)

Operator **re-granted** the autonomous run (same standing grant + carve-outs as the stopping-point entry above), with two hard gates reaffirmed: **no new dependency without an operator vet** (STOP → `cargo tree` → flag — the jsonschema-egress catch is the precedent), and the memory **narrative register** stays for operator review. No-new-dep bounded slices proceed autonomously in order: **Driver → config-from-`keel.lock` → `mw::cache` → `kernel::lock` → amplify-OFF.** Operator-only acts (frozen contracts / goldens / the seal / global toolchain) remain non-self-authorizable. Halt + alert at ~90% context.

- **Bootstrap / orientation (read-only).** Fresh session; ran the full INIT_PROMPT bootstrap — read WAKE_UP (all parts), STATE, WORKLOG, the canon, `CLAUDE.md`, `AUTONOMY_CHARTER`, `GENESIS-ARC`, the two memories, and the frozen surfaces (`golden.json` + `.frozen.json`, the ten contracts, `keel.lock`) in full; then verified state by artifact. Reconciled two doc-vs-artifact drifts (artifact wins): the `087e97c`→`ab5e0f0` HEAD staleness (a one-commit-stale snapshot — the INIT_PROMPT refresh — **not** a live mutation; reconciled to disk, both shells agree at `ab5e0f0`), and the WAKE_UP §0/§5.1 + STATE-line-57 prose lag (perception/memory/metrics already landed). Gate at start: HEAD `ab5e0f0` clean + pushed, **92 / 5 green**, clippy clean, freeze seal `db4377b3` green + active.
- **Slice — `svc::driver`: the initiative seam (canon §7 joint #8 / §8 loop step 1).** The frozen `Driver` joint had no impl; this lands the three defaults behind the one `poll() -> Option<Step>` joint: `UserTurnDriver` (FIFO queue of operator turns), `HeartbeatDriver` (perpetual tick; pure clock-injected `tick()` for deterministic tests — reads a clock at L4, the L0 contracts stay clock-free), `WatchDriver` (poll-based watch-on-change over a caller-owned probe token, so the genome takes no dep on the watched source). Each stamps `Step.source` (canon §7). Turns KEEL from "responds to user turns" into **a self that acts** (3 of the 4 real projects act without a user turn — DAVE outreach / TARS PULSE / The Box SILENCE), and gives the persistent memory + `metrics` a multi-turn signal to exercise. **Proves the §23 seam falsifier** — one joint expresses user work AND a heartbeat AND watch-on-change. **No new dependency** (`std::{time,sync,collections}` only; verified the crate's deps unchanged). **Scope:** the Drivers; the daemon select-loop (poll drivers in priority order + run each emitted `Step` through `kernel::engine`) is the deliberate follow-on (it owns run-execution, not the initiative seam). **+5 tests** (FIFO-drain · once-per-interval cadence · real-clock poll · watch-on-change · the §23 three-modes-one-seam capstone). **Gate: 97 passed / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Doc-reconcile (same change).** Folded in the cheap reconcile the bootstrap surfaced: STATE.md's stale "Next (sequenced)" line now points at this WORKLOG's authoritative next-moves; WAKE_UP §0 (where-it-stands + next-slice) and §5.1 (the Step-2 "Next" clause) refreshed to current. WAKE_UP **parts** edited (LF) + WAKE_UP.md **re-stitched** (CRLF) so the stitched file stays byte-in-sync with its parts (working-tree integrity — verified the re-stitch reproduces the file byte-for-byte before editing).
- **Slice — the daemon select-loop (`kernel::engine`, canon §8 `select(drivers).poll()`).** Finishes what the Driver started. `select(drivers, ctx)` polls the drivers in **priority order** (array order) and returns the first `Some(Step)`; `Engine::tick` runs that one Step through the full loop; `Engine::run_until_idle` is the **bounded** wrapper — ticks until every driver is idle or `max_ticks`, each turn under a **distinct `trace_id`** (`{base}-{n}`) so it checkpoints as its own run (N `metrics` rows + N Tape entries) while cost accumulates in the shared `ctx` (I4). `request_from_step` bridges a Step's `content` → the user turn. This turns KEEL from "responds to a user turn" into **a self that acts**, and gives persistent memory + `metrics` the real multi-turn signal. **Scope (per the operator):** the loop logic, built + tested — the continuously-running daemon (idle = sleep, run forever) is a thin L5 follow-on, **deliberately not started**. Lives in `kernel::engine` (L1; `Driver` is L0, so layer-correct) — **no new dep** (`std` + contracts). **+6 tests** (priority select · all-idle · request mapping · one-turn tick · bounded drain with distinct runs + cost accumulation · the `max_ticks` termination bound). **Gate: 103 passed / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Plan update + doc-bug fix (same change).** Operator review **dropped** `mw::cache` (DeepSeek caches server-side; Anthropic `cache_control` is an adapter concern; §22 speculative for now), `kernel::lock` (a no-op until the `sha256:` pins are set), and `amplify`-OFF (the §8/§23 premature-build) — each waits for a real trigger; **authorized** config-from-`keel.lock` (next). **Resolved the `keel.lock` governance question:** it is **config, not a frozen surface** — only the ten contracts + goldens + the seal are frozen; the agent may make keel.lock structural/config edits (flag them), only the `sha256:` pins are operator-only. Fixed the bootstrap's over-classification in `_run_state/INIT_PROMPT.md` (Phase 2). Refreshed STATE.md (banner/slice-log/Next) + WAKE_UP §0/§5.1 (parts edited + WAKE_UP.md re-stitched, CRLF, byte-in-sync).
- **Slice — config-from-`keel.lock`: substrate config is now keel.lock-driven (operator-authorized).** Retired the hardcoded launch consts (`LLAMA_EXE`/`LLAMA_MODEL`/`LLAMA_MMPROJ`) + the `max_tokens=2048` literal from `keel/src/lib.rs`. `kernel::manifest` now parses two previously-ignored sections — `servers` (llama_cpp `path`/`exe`/`endpoint`, `models_dir`) + `substrate.llm_vision` (`file`/`mmproj_file`) — plus a per-tier `max_tokens` (serde default 2048), and exposes `llama_exe()`/`llm_model_path()`/`llm_mmproj_path()` helpers that join dir + on-disk filename (the `id` is a normalized logical id, **not** the filename — so keel.lock now carries explicit `file`/`mmproj_file`). `resolve_local_endpoint`/`build_tier` take `&Manifest`; the cold-start reads the paths and **fails honestly** (`SUBSTRATE_UNRESOLVED`) if unconfigured rather than launching a guessed binary. **keel.lock edited** (added `exe`/`file`/`mmproj_file`/`max_tokens`, operator-authorized; **no `sha256:` pin fabricated** — keel.lock is config, not a frozen surface). Values match the old consts exactly (the manifest path-suffix assertions prove it), so this is a config relocation, not a substrate change. **No new dep** (serde parses; `Path` is std). +manifest test assertions (real-keel.lock parse · derived-path suffixes · absent-section → None). **Gate: 103 passed / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed.
- **Operator gate reached → operator said proceed.** At the config-from-keel.lock commit the no-dep queue was exhausted (Driver · daemon · config banked) and I flagged the remaining new-dep slices for a vet. The operator confirmed context healthy (54% of 1M) and **widened the grant: proceed to #1 + whatever else, decide autonomously — including dependency decisions — but log every decision to this permanent log.** So the run continued; deps are now agent-vetted + decided + logged here (a heavy/questionable one still gets flagged), not blocked on a synchronous vet.
- **Slice — perception capture (model-free core): real dHash + `FrameGate` + the `see()` retina (canon §12).** **Autonomous dep decision (logged):** WORKLOG #1 named "an `image` crate for real dHash," but dHash from a *captured* frame is pure arithmetic (luma + average-pool to 9x8 + adjacent-bit compare) — a raw frame is already pixels, no decode — so **no `image` crate was added** (file decoding is a cell/periphery concern, not the genome core; the leaner choice). Added `ChangeGate::dhash(luma,w,h)` / `dhash_rgba(rgba,w,h)` (the model-free hash the gate dedups on; uniform frame → 0; guarded on zero-dim/short buffers), a stateful `FrameGate` (remembers the last *emitted* hash, so slow drift still eventually crosses the threshold; first frame = baseline), and the **`see()` retina** mirroring `hear()`: dHash-gate → an Image `Percept` on visual change (static → `None`, GPU-free; the frame rides the cognition protocol as image content downstream — image forces `local`, I3; **no tier call in the retina**). **No new dep** (`std` + the existing `serde_json`). +5 tests (uniform→0 · gradient≠flat · rgba==luma · `FrameGate` emit-on-change · `see()` gates static / emits an Image `Percept`); fixed one `clippy::manual_checked_div` to keep the zero-warning bar. **Gate: 108 passed / 5 ignored, clippy clean, zero contract/golden edits, freeze seal unmoved.** Pushed. **Deferred (dep-gated, its own slice):** the OS-capture `PerceptionSource` over a real screen/mic (`scrap`/`windows`/`cpal`-class) — that's where the real perception dependency lives.
- **Operator steer → OS-capture device (proceed autonomously + log; deps decided autonomously).** Operator confirmed context healthy (54%), gave the embedder/reranker/privacy model HF links + `C:\models\`, said **privacy is least important (anytime/last)** and **OS-capture is probably more useful even than the embedder**, and to proceed at my discretion + log everything. Acted on it below.
- **Slice — ears OS-capture device: the `cpal` microphone (KEEL's FIRST dependency) + a no-dep VAD/WAV core.** **Autonomous decision (logged):** chose the **ears (mic) over the eyes (screen)** — researched the screen-capture crates (web) and they're all heavy/platform-coupled (`xcap` pulls the `image` crate I'd just deliberately avoided; `windows-capture` pulls the large `windows` crate), whereas `cpal` (the standard Rust audio crate, 0.17) is lighter, doesn't pull `image`, and **completes the already-built whisper + `hear()` chain**. **First new dependency: `cpal` 0.17**, added **behind an opt-in `mic` feature** (`keel-adapters`) so the **default genome core pulls no audio dep** (the minimal-core thesis — only a consumer that hears enables it). Landed: `keel-adapters::mic::Microphone` (default input device → mono `i16`, blocking record, **sovereign-local** — raw audio never leaves the box, I3), the no-dep `keel-adapters::write_wav_i16` (16 kHz mono RIFF/WAVE for whisper), and the no-dep `ChangeGate::voiced_ms` (energy VAD — raw samples → voiced ms, the signal `audio_voiced` gates on). +2 no-dep tests (VAD silence/speech · WAV header); the live mic capture is `#[ignore]`'d (operator-verified on a real mic, the whisper pattern). One cpal-0.17 API fix (`sample_rate()` returns `u32`, not `SampleRate`). **Gate: 110 passed / 5 ignored (default), clippy clean; `--features mic` compiles + clippy-clean + the live capture `#[ignore]`'d; zero contract/golden edits, freeze seal unmoved.** Pushed. **Deferred (thin follow-ons):** the `listen()` ears-retina (mic → VAD → WAV → whisper → `Percept` bundling; needs the `mic` feature in keel-services) · the eyes/screen capture device (heavier dep — flag the crate first).
- **Operator correction (logged) — perception is FIRST-CLASS, not "heavy periphery."** I mislabeled the eyes/screen slice: conflated **vision** (the model *seeing* — native Qwen3.5-9B early-fusion; `mmproj` is *its* projector, already wired via `local_llama` image_url + the `see()` retina; FREE) with the **screen pixel-grab** (the only new dep — a capture feed, parallel to `cpal`/mic), and called it "heavy" + a "weight on the core" — wrong on both (a feature-gated capture feed never touches the default core). The operator corrected the **priority inversion** (it's in WAKE_UP §2.8/§2.9/§3.5): perception (eyes = native Qwen, ears = Whisper) is baked-in genome — KEEL exists *so apps reuse this wiring* (NightScribe fused Qwen-vision + Whisper *before KEEL*; the .NET-of-AI-apps case). The §16 anti-bloat refusal targets a vertical's apparatus, **never the core's own senses.** Burned into the always-loaded cross-session memory (`keel-perception-is-first-class`) + WAKE_UP §1 anti-drift **item 11**. **Next:** build the eyes screen-capture **confidently**, feature-gated like the ears (a lean raw-buffer grabber → the native vision model — no VLM, no `image` crate).
- **Slice — eyes OS-capture device: `xcap` screen grab → the native Qwen vision (the corrected framing applied).** Built **confidently** as a first-class sense: `keel-adapters::screen::ScreenCapture::grab()` (xcap 0.8; primary monitor → raw RGBA8) **behind the opt-in `screen` feature**. Feeds the **already-built** `dHash`/`FrameGate`/`see()` → the **native Qwen3.5-9B vision** (`local_llama` image_url) captions it — **no separate VLM, no captioning organ** (unlike ears + whisper; the eye IS the brain, canon §2.8). So eyes was *smaller* than ears (only the grab; the gate + retina + vision already existed). **2nd new dep: `xcap` 0.8**, feature-gated. *(Honest note vs the line above: I'd planned a "no-image" raw-buffer grabber but chose `xcap` — the cleanest single-frame screenshot API — which pulls `image` for its `RgbaImage`. Feature-gated, the **default core stays `image`-free** (the toggle model §3.5); I convert the frame to raw bytes for the existing `dhash_rgba`. So "no `image` in the core" holds — `image` rides only the `screen` feature.)* Live grab `#[ignore]`'d (operator-verified, needs a display); the `xcap` API (`Monitor::all`/`is_primary`/`capture_image` → `RgbaImage.into_raw()`) compiled first-try. **Gate: 110 passed / 5 ignored (default), clippy clean; `--features screen` compiles + clippy-clean (+1 ignored live); zero contract/golden edits, freeze seal unmoved.** Pushed. **Both senses' capture devices now exist** (mic + screen), each feature-gated, feeding the model-free gates → the native Qwen vision / Whisper.
- **Meta-slice — the autonomy mechanism (the operator's #1 ask: never do another handoff).** He named the true rate-limiter: being the manual context-manager + handoff every session. **Diagnosis (honest):** it is NOT context size — it is a missing **reconstitution + respawn loop** (KEEL's own thesis: the self = the durable record + the discipline of re-reading it). Built it: **`_run_state/ROADMAP.md`** (the single durable NOW→DONE blueprint — the autonomy contract §0, every slice dependency-ordered with done-criteria + operator-gated/unknown flags, the DONE definition, the perpetual-polish mode, the ISSUES queue the loop routes around); **`_run_state/AUTOSTART.md`** (the standing directive a session executes: reconstitute → ROADMAP loop → gate/bank/push → halt-at-90% + exit → DONE/STALLED sentinels → the inviolable guards); **`tools/keel-autoloop.ps1`** (the external respawn supervisor — the temporary external `Driver`: loops `claude -p <AUTOSTART> --dangerously-skip-permissions`, each fresh session self-bootstraps + works slices to ~90% then exits, respawns until DONE/STALLED/no-progress-stall/cap; logs `.keelstate/autoloop.log`); **`_run_state/AUTOSTART_SETUP.md`** (the ONE-TIME operator wiring — SessionStart hook `matcher:""` fires on the `compact` source too = **auto-rehydration** · PreCompact snapshot · the unattended permission policy; hook/headless schema **verified** against the Claude Code docs via a claude-code-guide agent — `--dangerously-skip-permissions`, not the invalid `bypassPermissions`). **Deep alignment:** KEEL *is* the answer to its own build problem — the meta-loop dogfoods KEEL (docs = Memory/Tape · gate+I5 = externality · slice protocol = engine · supervisor = Driver); the external scaffold is temporary, until the in-KEEL daemon (ROADMAP A2) hosts its own loop. **Honest bound (skeptic pass):** not "zero intervention to perfection" — indefinite autonomous progress banking gate-passing slices, surviving compaction, flagging the few operator-only acts (routed around, never blocking), until quota/power or true-DONE. Commits `8c9b5bb` (ROADMAP) + `34b334e` (mechanism), pushed; STATE ⛑ + banner wired to ROADMAP/AUTOSTART.

---

## 2026-06-14 — autonomous run (perpetual, self-perpetuating): A1 retina wrappers  (Claude Opus 4.8, 1M ctx)

First session under the **perpetual** mandate (ROADMAP §0 loop + AUTOSTART — no manual handoff; a compaction is a *rehydrate* event, not a stop). Reconstituted via the ⛑ protocol: WAKE_UP (all parts) → STATE → ROADMAP → AUTOSTART → this WORKLOG; verified by artifact from PowerShell — HEAD `0b93199` clean + pushed, **110 passed / 5 ignored**, clippy clean, freeze seal `db4377b3` green (the `goldens_match_the_frozen_hash` gate is among the green tests). Next actionable ROADMAP slice = **A1** (deps met; A3/A5/A6 are `[G]` operator-gated → routed around).

- **Tooling lesson (logged to cross-session memory).** Diagnosed a false `cargo test` exit-1 before trusting it: the **Bash tool is git-bash** on this box, and wrapping `powershell -Command` through it mangles `$LASTEXITCODE` + string quoting, and merging cargo's stderr (its *normal* progress stream — "Compiling"/"Finished"/test names) surfaces as a PowerShell `NativeCommandError` → exit 1 **even with every test passing**. Switched to the dedicated **PowerShell tool** (the native MSVC path CLAUDE.md/WAKE_UP require) → clean exit codes (`test_exit=0`, `clippy_exit=0`), no false failure. Saved as memory `keel-use-powershell-tool-for-cargo` (verify-by-artifact: the build was green all along).
- **Slice A1 — `listen()` + `see_screen()` retina wrappers (`svc::perception`, canon §12).** Bundled the existing capture devices + change-gates + organs into the two afferent retinas, each behind its **forwarded feature** (the minimal-core thesis §3.5 — the default genome core pulls no capture dep). `listen()` (`#[cfg(feature="mic")]`) = `Microphone::capture` → `listen_from_samples` (VAD-gate via `voiced_ms` → **silence short-circuits**: no WAV written, whisper never touched → `resample_to_16k` → `write_wav_i16` → `hear()` → Audio `Percept`). `see_screen()` (`#[cfg(feature="screen")]`) = `ScreenCapture::grab` → the already-tested `see()` (dHash `FrameGate` → Image `Percept` on visual change, static → `None`). **Sovereign + local** — raw audio/frames never egress (I3); the changed frame rides the cognition protocol to the native Qwen vision downstream (image forces `local`). **Autonomous decisions (logged):** (a) added a **no-dep `resample_to_16k`** (linear, pass-through at 16 kHz) — the mic captures at the device's native rate and `mic.rs` explicitly asks the caller to resample for whisper's 16 kHz mono; a polyphase anti-alias filter is a later enrichment if ASR quality ever warrants (whisper is robust to linear resampling of speech). (b) **Factored the hardware-free `listen_from_samples`** (always-compiled) so the **silence-gating** path is unit-tested *without a mic* — the voiced path needs the whisper binary, so it's the live `#[ignore]`'d test (the established `live_*` pattern). Forwarded `mic`/`screen` from `keel-adapters` into `keel-services` Cargo features. **No new dependency** (the cpal/xcap deps ride only the opt-in features; `resample_to_16k`/`listen_from_samples` are pure std + existing crates). **+2 always-compiled tests** (resample pass-through/downsample/upsample + empty/zero guards · silence-gated-no-transcribe-no-WAV) + **2 feature-gated live `#[ignore]`'d** (real mic capture+transcribe under `--features mic`; real screen grab+emit under `--features screen`). **Gate: 112 passed / 5 ignored (default), clippy clean on default + `--features mic` + `--features screen`, both feature test-builds compile, secret-scan of the staged diff clean, zero contract/golden edits, freeze seal `db4377b3` unmoved.** Pushed. ROADMAP A1 → `[x]`; next actionable = **A2** (the Driver daemon, L5).
- **Slice A2 — the Driver daemon (L5): `keel daemon`, canon §8.** The thin L5 wrapper the kernel docstring explicitly defers here (the bounded loop logic + 8 tests already live in `kernel::engine`). `keel daemon [--max-ticks N] [--interval MS] [--watch PATH] [--prompt …]` wires a `HeartbeatDriver` (+ an optional file `WatchDriver`) and runs the §8 loop: `tick` (select → run → verify → checkpoint → Tape) → on idle, sleep `--interval` and re-poll. **Bounded by default** (`--max-ticks 1`, terminates — the autonomous-safe default); `--max-ticks 0` or a `--watch` without an explicit bound = perpetual (until interrupted). Exposed `keel::Engine::tick`/`run_until_idle` (passthroughs to the tested kernel loop) + the pure, CI-tested helpers `watch_token` (file mtime+len → a comparable change token for the `WatchDriver` probe — model-free, the dHash-gate discipline) and `daemon_perpetual` (the bound decision). **No new dep.** **+4 tests** (2 keel-lib: `watch_token` change/none, `daemon_perpetual` rules; the loop itself is covered by the kernel's 8). **Lived BY ARTIFACT** (verify-by-artifact, NOT a hanging live capture): a `--max-ticks 2` run self-drove 2 heartbeat turns end-to-end against local Qwen — distinct traces `28c2fa8d5250-0`/`-2` (attempt 0 ran, 1 idled, 2 ran = the bounded select-loop), local tier, $0, ok:true, each checkpointed (index) + Taped + audited; `keel metrics` counted the turns. **Gate: 114 passed / 5 ignored, clippy clean, secret-scan clean, zero contract/golden edits, freeze seal `db4377b3` unmoved.** Pushed. ROADMAP A2 → `[x]`.
- **Operator interrupt → kill switch + PIVOT (2026-06-15).** While verifying A2 *live*, a 2-tick daemon capture sat at 10m44s. **Diagnosis (artifact, not recall):** the daemon ran fine and exited fast (traces `-0`/`-2` on disk, no `keel.exe` left running); the hang was the **shell capture** — on Windows cold-start, the detached llama-server inherits keel's stdout/stderr pipe handle, so `… 2>&1 | Out-String` blocks on an EOF that never comes. A gate-green detach patch in `kernel::lifecycle::launch` (stdin→null + `DETACHED_PROCESS`) did **not** resolve the live hang (and the tool timeout didn't fire either), so per the operator I **reverted it** (keep the checkpoint honest), filed **ISSUE-8** (verify-by-artifact + always TTL the run; real fix deferred), and **pivoted off live-daemon verification** — A2 is already proven by artifact. **Operator standing rules captured (survive compaction):** (1) **TTL everything** — bound every shell/live run with an explicit kill switch, never start something unbounded; (2) **when stuck ~twice, pivot** — record state, file an ISSUE, move to the next actionable slice, leave the stuck branch for later. Encoded in **ROADMAP §0.9** + memories `keel-ttl-everything-and-pivot` · `keel-live-run-kill-switch`. **Pivoting to the perpetual-memory work (A6 — the narrative register + consolidation)** per the operator's steer; most of it is model-free structural work (testable with stubs), so it sidesteps the live-model-hang trap entirely.
- **Slice A6.1 — perpetual memory: the Ring-3 narrative register (`svc::memory`, canon §11).** Advanced the canon's flagged **highest-risk seam** with a deliberately **safe, model-free, additive** cut behind the **frozen `Memory` trait** (zero contract change — the enrichment lives entirely in `FileMemory`). What landed: (1) the Ring-3 **narrative register** — `narrative()`/`set_narrative()` over a sibling file of the Tape (`<tape_stem>.narrative.md`, keyed to the stem so distinct Tapes never cross-talk) — the **model-authored, lossy compressed arc**, kept **separate from the lossless factual Tape** (the I5 boundary made structural: the narrative carries *arc + intent*, the Tape carries *facts* — a model may not author its own ground truth); (2) `assemble` now **layers Ring-0 soul → Ring-3 narrative → Ring-2 recent turns**; (3) `consolidate()` returns a `Step` carrying a real **self-interview / forward-narrative** prompt (straight from `docs/proposals/perpetual-memory.md`: the durable cause→effect arc + current state + anticipated-Q&A for the successor, with the I5 boundary stated in the prompt) over the prior narrative + recent turns — the model authors the *result* when the Step is routed. **No new dep; all model-free + unit-tested** (no live-model dependency — the deliberate sidestep of the daemon-verification trap). **+3 tests** (narrative injects into assemble & survives separately from the Tape · the consolidate prompt folds in the prior narrative + recent turns + the I5 boundary · the narrative path is a distinct per-Tape sibling). **Deferred → A6.2** (model-dependent / higher-risk, for a careful/bounded pass): wire the consolidation loop (a Driver/threshold emits `consolidate()` → the engine generates the narrative → L5 stores it via `set_narrative`), the **cold-eyes validation** Step (periodically diff the narrative against the Tape, I5), a swappable consolidation policy, Ring-1/Ring-4. **Gate: 117 passed / 5 ignored, clippy clean, secret-scan clean, zero contract/golden edits, freeze seal `db4377b3` unmoved.** Pushed. ROADMAP A6 → `[~]` (A6.1 done; the operator's steer lifted the ISSUE-3 gate for the safe structural cut).
- **Operator fork (A4) → deferred; pivoted to B2.** Started A4 (re-home the no-SSN I5 stopgap → an I3 output rung), read `mw::privacy`, and surfaced a real **privacy-policy fork** in the operator's flagged forward-design area: the middleware sees only request/response (not the turn's `sovereign` class), so the genome needs ONE default — **mask output PII on all tiers** (keeps PII out of the now-persistent Tape/ledger/egress, but masks a local sovereign answer's own PII) **vs egress-only + audit-local** (sovereign answers intact; PII can sit in the local Tape). A6.1 made it sharper (the Tape persists outputs). Asked the operator (he's present, his sensitive area — collaboration, not handoff-tax); he chose **Defer A4 → build B2 instead.** Logged as **ISSUE-9**; A4 → `[G]` pending the policy.
- **Slice B2 — the flywheel feed: `svc::trace_sink::FileTraceSink` (canon §8 step 9 / §5 reversibility gate).** The first Stage-3 slice. A passed `VerifiedTrace` → an append-only JSONL **distill corpus** (`.keelstate/traces/corpus.jsonl`), with **secrets/PII SCRUBBED before the trace is ever feedstock** — the reversibility gate (a secret baked into a distilled LoRA is irreversible; undo = retrain, so the trace→distill path MUST scrub first). The scrub **reuses the same `Redactor`** (rungs 1–2) the I3 egress mask uses — **one source of truth for "secret"**, so the corpus can never carry what egress would have masked (added a `keel-middleware` path dep to `keel-services`; services→middleware is layer-legal + acyclic — verified no cycle). Scrubs exactly the (prompt, completion) a distill run trains on: `step.content` + `result.content`/`reasoning_content`; the trace structure survives the scrub (still a usable training record). Wired into the engine's **existing** emit-on-pass (L5 `trace_sink: Some(FileTraceSink::new(TRACES_PATH, redactor.clone()))` — the engine already emitted on pass; this gives the sink a home + the scrub). **No new external dep** (intra-workspace path dep only; verified `cargo tree` adds nothing external). All model-free + unit-tested. **+2 tests** (a key in the completion / ssn in the prompt / email in the reasoning are all scrubbed before write, structure intact · one JSONL line per trace + clean text written verbatim). **Gate: 119 passed / 5 ignored, clippy clean, secret-scan clean, zero contract/golden edits, freeze seal `db4377b3` unmoved.** Pushed. ROADMAP B2 → `[x]`.
- **Slice B4 — `svc::distill` (out-of-band hand-off).** `keel-services::distill` flattens the scrubbed corpus → chat-format `{messages:[user,assistant]}` JSONL (`training_pair` / `export_training_jsonl`), + `keel distill-export [--in][--out]` writes the training file. Corpus is scrubbed at write (B2), so the export carries no secret; LoRA training stays external (Unsloth, §16-refused). +3 tests. **122/5 green, clippy clean.** Pushed. ROADMAP B4 → `[x]`. *(Operator directive received: cut meta-overhead, no more questions ever, kill-timer + round-robin real work, model-free first. WORKLOG/STATE entries now terse.)*
- **Slice A4 — I3 output rung; no-SSN stopgap retired.** `mw::privacy` now scrubs PII from the response on EVERY tier (audited I1) — model output PII never reaches the persistent Tape/ledger/egress/display. Retired the engine's no-SSN I5 baseline (wired `None`; the slot stays a generic always-on extra-oracle seam, excluded from #3). **Decided mask-all-output** myself (state-hygiene default; common-sense per directive, no ask) — resolves ISSUE-9. +1 test. **123/5 green, clippy clean.** Pushed. ROADMAP A4 → `[x]`.
- **Slice E1 — conformance coverage (port-readiness).** `docs/conformance-coverage.md`: maps every joint + invariant → its golden family or structural unit test; verdict = the 6 golden families are a complete *behavioral* conformance layer, structural joints carry no golden by design; 2 documented gaps (`recall` until A3, `ToolHost` until D3). Doc-only. Pushed (`52c4dd2`). ROADMAP E1 → `[x]`.
- **Slice A3 (first pass) — embedder + recall (Ring-4).** Decided ISSUE-1 myself (no ask): **brute-force cosine, NO `sqlite-vec`** (L1 memory is small; sqlite-vec = deferred scale opt). Built `keel-adapters::Embedder` (HTTP `/v1/embeddings`, Qwen3-Embedding-0.6b, **sovereign-local** — vectors are invertible, never egress, I3; live `#[ignore]`'d) + `keel-services::recall` (`cosine`, brute-force `recall_top_k`, `Fingerprint{embedder,dim}` + `should_rebuild`). **`GOLDEN_RECALL` deterministic case GREEN** (`passes_golden_recall_fingerprint`: mismatch → no-serve + rebuild-from-ledger). **No new dep.** +3 tests. **Remaining:** the recall@k/ndcg **uplift** cases = the §23 C1/C2 falsifiers (real embeddings, measured later); wire Ring-4 retrieval into `FileMemory::assemble` (live embed — deferred). **126/6 green, clippy clean.** Pushed. ROADMAP A3 → `[~]`.
- **Slice A3 Ring-4 — semantic recall wired into memory (real feature).** Added the `Embed` seam (`keel-services::recall::Embed`, impl'd for `keel_adapters::Embedder`, stub-testable). `FileMemory::with_embedder(embedder, fp, k)` wires Ring-4: **embed-on-record** appends `{text, vec}` to a `.vec.jsonl` sidecar; **assemble** embeds the current query → cosine top-k from the sidecar → injects "Relevant earlier (semantic recall)" (deduped vs Ring-2). **Fingerprint-mismatch clears the stale sidecar** (never serve a mismatched index — `GOLDEN_RECALL` intent; full re-embed-from-ledger deferred). **Opt-in** — the genome default wires no embedder (no live-embed dependency); a cell turns it on. Lock scoped so no Mutex is held across the embed await. **No new dep.** +3 tests (stub-embedder recall ranking · fingerprint-clear · the seam). **128/6 green, clippy clean.** Pushed.
- **Slice A6.2 (partial) — `keel consolidate`: closes the perpetual-memory loop.** L5 trigger reusing the tested pieces: build `FileMemory` → `consolidate()` (the self-interview prompt over recent turns) → route it through the engine (made `consolidate()` **sovereign → local**: a memory narrative may carry private context, privacy beats a cloud summarizer) → store the model-authored narrative via `set_narrative` (Ring-3). Closes A6 structurally (the loop now runs end-to-end). **Live generation = model-dependent → deferred** (operator/bounded verify; not risking the cold-start hang). Compiles + suite green; pieces individually tested. **128/6 green, clippy clean.** Pushed. (Remaining A6.2: cold-eyes validation, daemon auto-trigger, swappable policy.)
- **Verified — A6.2 LIVED end-to-end (perpetual memory closes).** `keel consolidate` cold-started llama-server, routed the consolidation Step **sovereign → local** ($0), the model authored a coherent 589-char Ring-3 narrative (a "Durable Arc" + "Current State" summary of the Tape's turns — the "42" fact + heartbeat ticks), and `set_narrative` stored it to `.keelstate/tape/tape.narrative.md`. **Exited clean, NO hang** — verified by artifact. **ISSUE-8 workaround found + works:** run live model commands via `Start-Process -RedirectStandardOutput <file> -PassThru` + `$p.WaitForExit(ms)` → `$p.Kill($true)` (a **reliable self-kill timer**) and a **file redirect, NOT `| Out-String`** (the pipe was what hung on the detached server's inherited handle). No code change — a verification milestone. The loop now runs end-to-end: turns → Tape → consolidate → Ring-3 narrative → injected back via `assemble` next turn.
- **Verified — the WHOLE flywheel LIVED end-to-end (real data).** Ran 3 real turns via the safe pattern: `q1` "capital of France"→**local** "Paris"; `q2 --kind core-wire`→**cheap-API/deepseek** (real answer, $0.0004 — routing works); `q3`→**local** "Red". Then `keel metrics` over the real index: **turns=18, escalation_rate=0.000, rework_rate=0.056, by_tier=local:17/cheap-API:1, $0.0004**. `keel distill-export` → **4 chat-format training pairs** from the scrubbed corpus (sample = the consolidation turn); **corpus secret-scan clean**. So the entire genome is proven live: DifficultyRouter (local+cheap-API) · the engine loop · Memory (Tape persist + Ring-3 consolidation) · I5 verifier (passed→corpus) · B2 scrubbed flywheel feed · B4 training-pair export · B3 metrics · A4 output-scrub (corpus clean). **Preliminary falsifier decisions from real data:** **C4** rework 5.6% < 10% → PASS; **C5** KEEL routed 17/18 to free local (~78% cheaper than cheap-API-everything) → KEEL-favorable. (Small N; ROADMAP C4/C5 → `[~]` with the prelim decision recorded.)
- **Slice A6.2 daemon auto-trigger — `keel daemon --consolidate-every N`.** The daemon now self-consolidates every N ticks: extracted `do_consolidation` (shared by `keel consolidate` + the daemon), and the loop runs it on a distinct `{base}-consolidate-{n}` trace_id every N turns. Closes the canon §8/§11 vision — a self that **acts AND compresses its own memory** autonomously. **128/6 green, clippy clean.** Pushed.
- **D1 SCOPED (the first cell, NightScribe on KEEL).** Read NightScribe (C#/.NET, Phases 0-3 done): it **independently hand-rebuilt KEEL's exact pieces** (Qwen vision + Whisper + dHash dedup + a deterministic "no-LLM-judges-itself" oracle + an intent-memory routing db) — the .NET-of-AI-apps thesis in the flesh. **Boundary is clean** (confirms the genome's altitude): eyes/ears/perception-gate/route/oracle/memory/constrained-decode all come FROM KEEL; only meeting periphery (dual-track me/them capture → KEEL's `source` field, MP4 import, minutes synthesis, GUI, eval) is cell-written. Full boundary map in ROADMAP `[~] D1`. **The cell BUILD is the next major effort** (cross-language, consumes KEEL over `serve_openai`) — best begun with fresh full context.

---

## 2026-06-15 — SESSION CHECKPOINT (comprehensive: buildable genome done + validated live)

**This session: 11 real slices + 2 live-validation milestones, all gated (128/6 green, clippy clean) + pushed** (`0b93199`→ this run's tip; `git log`). Perception retinas (A1) · daemon + self-consolidation (A2/A6.2) · memory narrative + recall + Ring-4 (A6.1/A3) · flywheel feed + distill-export (B2/B4) · privacy output rung (A4) · conformance map (E1). **Whole flywheel proven LIVE** (real turns route local+cheap-API; memory consolidates a real narrative sovereign/local $0; corpus exports clean training pairs; C4/C5 prelim PASS). The daemon-hang is solved (ISSUE-8 workaround). The operator's directives are baked in (TTL+pivot, never-ask, terse docs — ROADMAP §0.9 + memories). **The directive's end-condition is MET: the bulk + low-hanging fruit are done.** **NEXT for the perpetual loop: the D1 cell build** (NightScribe on KEEL — scoped), then A5/C1/C2/E2 — the larger/heavier tier. State fully checkpointed for a fresh-context continuation.

---

## 2026-06-15 — overnight continuation (operator asleep; directive baked durable)

Operator gave the standing directive (real work / never-ask / TTL+pivot / model-now / common-sense) and went to bed; the run continues. Baked it into `_run_state/OPERATOR_DIRECTIVE.md` + the reconstitution chain (AUTOSTART §0/§1, STATE ⛑) + the cross-session memory, so it survives compaction (commit `acf9f9b`).
- **Slice A6.2 cold-eyes — `keel cold-eyes` (the I5 capstone for memory, canon §10.2).** `FileMemory::cold_eyes_prompt` builds a **fresh, uninvested** validation pass: diff the model-authored Ring-3 narrative against the lossless Tape (ground truth) → flag claims the Tape doesn't support, else reply CONSISTENT (catches the self-curation blind spot; the Tape validates the narrative, never the reverse — I5). `keel cold-eyes` CLI runs it sovereign→local. Structurally identical to the lived `consolidate`. +1 model-free test. **129/6 green, clippy clean.** Pushed.
- **Verified — cold-eyes LIVED + caught REAL drift.** `keel cold-eyes` cold-started, ran sovereign→local, and flagged that the consolidated narrative **over-reached** vs the Tape: it claimed an explicit "retrieval verification (confirming '42')" arc step and "only the heartbeat tick pending" — neither fully supported by the Tape. Exactly the I5 point proven live: the model-authored narrative is lossy + drifts; a fresh pass against the lossless Tape catches it ("a model may not author its own ground truth"). The whole perpetual-memory I5 story is now lived end-to-end (consolidate → narrative → cold-eyes catches drift → recall).
- **Phase-C first-pass status (real data / honest deferral).** **B3** → `[~]`: escalation_rate **0.000** over 18 live turns = the canon base case (no escalations; ignition is upside); the trend-down needs the Stage-3 flywheel running (out-of-band trainer not run) — deferred. **C1/C2** → the embed organ + cosine recall + fingerprint golden are built (A3); the recall@k/ndcg **uplift** benchmarks need the embed model served + a labeled set = a focused live session (the embed-server setup is stuck-prone unattended → deferred per pivot-when-stuck, not blocking). **C4/C5** prelim PASS/KEEL-favorable (recorded earlier).
- **QC pass — multi-agent audit + fixes (ultracode).** Full backup (`C:\KEEL_backups\KEEL_20260615-022418.{bundle,zip}`), then a fan-out audit of the whole codebase vs the canon (48 agents, 3.07M tokens, 495 tool-uses) → `docs/AUDIT-2026-06-15.md` + `docs/RUN-2026-06-15.md` + `docs/PROJECT-STATE.md`. **Verdict GREEN** — zero critical/high; 28 raw → **17 confirmed** (5 medium, 8 low, 4 affirming-info) after adversarial refutation. Affirmed sound: the 10 contracts frozen, freeze-gate live+green, the layer rule holds in every crate, I1 unbypassable, I5 non-bypassable + non-vacuous on the self-driving loop. **Fixed the confirmed mediums + cheap lows (`130/6 green, clippy clean default+mic+screen`):** **M1/I5** — the `--tier` override now **refuses** `--sovereign` (non-local) / `--critical` / `--golden-ref` rather than silently voiding the I3 force-local gate + the I5 verifier (was a false-sense-of-protection footgun); **M2** — memory-maintenance turns (consolidate/cold-eyes, `source:"memory"`/`ty:"memory:*"`) are now **excluded from the lossless Tape** (kills the register-confusion + self-ingest loop) + a test; **M3** — the daemon **re-seeds each tick's budget headroom** so a perpetual *paid* daemon can't climb one shared per-task budget into a permanent `BudgetExceeded`; **doc-drift** — CLAUDE.md (freeze-gate ACTIVE, `svc::memory` shipped, metrics is a store-reader not `mw::metrics`) + the stale `lib.rs` `TraceSink` comment; **poison-policy** unified (driver/mic → recover, matching memory/sink). Docs committed `f9c031a`; fixes this commit. Pushed.

---

## 2026-07-09 — wake-up: June-17 tree anomaly RESOLVED · ISSUE-10 RESOLVED (supervised; operator present)

Operator returned ("awaken, bearings, recommendations"). Verified the 2026-07-06 SITREP state unchanged (git status byte-identical; freeze-gate live-green, seal `db4377b3`). Operator authorized three decisions: tree cleanup (stash route) · embed-model download · SUPERVISED mode this session.
- **Tree anomaly RESOLVED (stash):** the 8-path stale partial revert of 2026-06-17 (zero novel content — SITREP + GLM forensics concur) → `git stash push` "2026-06-17 stale partial revert (zero novel content; forensics: SITREP-2026-07-06 + GLM recon)". Undo = `git stash pop` (preserved for forensics). Tree = HEAD `6ac319d`.
- **Full gate re-confirmed on the restored tree: 130/6 green, 0 failed, clippy exit 0** (logs: `.keelstate/gate-{check,test,clippy}.log`).
- **Reviews made durable:** `_run_state/SITREP-2026-07-06.md` + `_brainstorm/` (GLM 6-agent recon 06-17 · Fable-5 canon hindsight review 07-01, **advisory/non-canonical, ruling = operator, piecemeal**) committed `28f55ae` (were disk-only).
- **ISSUE-10 RESOLVED (operator-authorized):** downloaded `qwen3-embedding-0.6b-q8_0.gguf` (official `Qwen/Qwen3-Embedding-0.6B-GGUF` Q8_0; **639,150,592 B byte-exact** vs content-length) to `C:\models`, then **smoke-tested LIVE** per the ISSUE-8 kill-switch pattern (llama-server :8090 `--embeddings --pooling last`, file-redirected, bounded, killed): `/v1/embeddings` → **1024-dim vector** — matches the keel.lock `dim: 1024` pin. **C1/C2/A3-live/Ring-4-live unblocked** (each still needs its focused live session).
- **`nul` junk file:** deleted (`\\?\` path) — then **re-appeared during the background cargo gate**: something in the build/test path does a cmd-style `> nul` redirect. That's a reproducible lead on the June-17-era artifact. Deleted again at session end; the hunt = a spawned side-task, not this session's scope.
- **Mode: SUPERVISED** (operator's pick; autonomous re-grant deferred). **NEXT: D1** (the NightScribe cell build — scoped in ROADMAP) · C1/C2 now unblocked · A5 last · v0.3.0 ruling pending (non-blocking). Docs this commit. Pushed.

## 2026-07-09 (same session) — OPERATOR REDIRECT: "solve memory" first; A7 Memory Autopilot proposed

Operator directive (live): *functionally perpetual, self-managing, contextually-relevant memory with zero operator intervention — not the flywheel; memory before D1.* Verified the gap at the call sites first: Ring-4 inert in production (`with_embedder` test-only; all four L5 `FileMemory::new` sites wire Ring-0/2/3), consolidation off unless `--consolidate-every`, cold-eyes manual, `assemble` count-based not budgeted (canon §7 "budgeted" unmet), Ring-3 a single overwritten narrative with no mid-tier register, cold-eyes verdict unacted-on.
- **Proposal authored:** `docs/proposals/memory-autopilot.md` — budgets (A7.1) · append-only five-field **episodes register** (A7.2, the L1–L4-cascade replacement with a re-open falsifier) · **Ring-4 on-by-default** over a lifecycle-resolved embed server + two-tier index (A7.3; ISSUE-1 brute-force kept, latency falsifier re-opens sqlite-vec) · L4 `MaintenancePolicy` triggers, no flags (A7.4) · cold-eyes-driven **self-correcting narrative**, one bounded regenerate + I1 event (A7.5) · optional Ring-1 + Ring-2-as-conversation (A7.6). All behind the frozen `Memory` trait — zero contract/golden edits. Acceptance falsifiers F-M1..F-M4 (O(1)-context-forever · plant-and-recall · zero-intervention lived run · drift→auto-correct→CONSISTENT).
- **ROADMAP:** Phase-A gains `[ ] A7` + six sub-slices, priority **before D1** (D1 becomes A7''s lived field validation). STATE banner cursor updated (NEXT: A7, then D1). REEL-v1.0 mapping recorded in-chat this session (protocol read in full from `C:\OUTREACH\BC_Canon\MAY2026\Deeper\REEL_PROTOCOL_v1_0.md`).
- Docs-only; gate untouched (130/6 green stands). This commit. Pushed. **NEXT: A7.1 (budgeted assembly) on the operator''s go.**

## 2026-07-09 (same session, operator grant: "build A7 start to finish") — A7 MEMORY AUTOPILOT: built + lived, all six slices

Supervised session, operator present. Six gated commits + one hardening commit, each `cargo test`+clippy green; **zero contract/golden edits throughout; seal unmoved. Final gate 155/6 green.**
- **A7.1 budgeted assembly** (`56d116a`): per-ring `TokenBudget` caps (chars-per-token proxy) enforced in `assemble` — Ring-0 verbatim always (REEL 3.4); newest turn always survives (trimmed, never dropped); budget reported on the frozen `AssembledContext`. O(1) context per turn.
- **A7.2 episodes register** (`877f410`): consolidation appends a five-field digest (`<tape>.episodes.jsonl`, append-only, never re-compressed — immune to compression-of-compression by construction); one generation → two registers; deterministic fallback stub flagged `parsed:false`.
- **A7.3 Ring-4 ON by default** (`34d86ad`): lifecycle gains embed-server resolve/launch (`--embeddings --pooling last`, own port from keel.lock); `build_memory` wires all four L5 sites; two-tier index (ALL episode vecs + bounded recent-turn window — ISSUE-1 brute force stays O(window+episodes) forever, latency falsifier re-opens sqlite-vec); lazy bounded backfill; cosine relevance floor. **LIVED:** first `keel` call cold-started the embed server (pid 23728, :8090) and backfilled the real Tape 0→13 vectors, $0.
- **A7.4 autopilot policy** (`36d3485`): L4 `MaintenancePolicy` (every-N / Ring-2 pressure / session-end; cold-eyes cadence) + `<tape>.maint.json` cursor; wired zero-flag into CLI session-end, daemon tick (legacy `--consolidate-every` kept as override), serve background (compare-exchange gate). **LIVED:** across 7 zero-flag CLI sessions the policy consolidated at exactly turns 1 and 5 (821/1388-char narratives + parsed episodes), idle between.
- **A7.5 self-correcting narrative** (`569a430`): cold-eyes verdict parsed; drift → ONE regenerate-from-ground-truth correction (drifted copy EXCLUDED, REEL 10.2) + I1 `MEMORY_DRIFT_CORRECTED`; drift-again-before-CONSISTENT → `MEMORY_DRIFT_PERSISTENT`, surfaced, never a loop.
- **A7.6 Ring-1 + conversation** (`b17e22b`): exemplar pool (`<tape>.exemplars.md`, anchor + recency rotation, budgeted, pool never pruned) + opt-in Ring-2-as-real-messages; kernel splices the frozen `AssembledContext.exemplars/.conversation` (+2 kernel tests).
- **Acceptance falsifiers:** **F-M1 PASS** (permanent test: 5k-turn Tape → bounded assemble, <2 s). **F-M2 PASS LIVED** (planted "codename BLUEFIN", buried beyond Ring-2 by six filler sessions → "Your project codename is BLUEFIN", carried by Ring-3 + Ring-4). **F-M3 PASS LIVED** (verified by artifact: episodes/cursor/audit). **F-M4 machinery PROVEN LIVED** — planted drifts ("Admiral Whiskers", "yacht Meridian") detected + erased + audited + bounded; the pending-drift confirm transition ("the drift correction held") lived. **Honest residual:** single-judge recall on ADVERSARIAL plants is stochastic on Qwen3.5-9B (the 2026-06-15 ORGANIC drift catch stands); mitigated with a 2-of-3 majority vote (local, $0, short-circuiting); upgrade trigger = a stronger local judge model; cloud judging declined by default (I3 — sovereignty beats judge recall on personal memory).
- **Six lived lessons, each caught by the falsifier runs and hardened same-session** (proposal 6.5): (1) maintenance-turn contamination — the engine injected the assembled narrative into cold-eyes/correction turns (narrative validating itself; correction re-anchoring on the drifted copy) → kernel now SKIPS ring assembly for `source=="memory"` steps (read-side twin of the M2 Tape-exclusion, +1 test); (2) cold-eyes window-blindness → episodes join the reviewer ground truth; (3) `<think>` leakage + mid-think truncation → strip closed blocks, unclosed = no-answer, local `max_tokens` 2048→4096 (keel.lock, config); (4) thin-substrate template parroting → format-contract-last + parenthesized never-copy hints + store-side placeholder guard; (5) corrector degeneracy (10-char stub) / sprawl (8.5k essay quoting the false claims) → corrector thinks + 40-char floor (a stub never clobbers) + never-mention contract; (6) judge stochasticity → 2-of-3 vote.
- **keel.lock (config, not pins):** `substrate.embedding.file/port` + local `max_tokens: 4096`. New direct dep: `serde` derive in keel-services (already in the tree). ROADMAP A7 + all sub-slices → `[x]`. Real-Tape narrative refreshed post-fixes (864-char clean arc). **NEXT: D1** (the NightScribe cell — consuming the autopilot as its field validation). This commit. Pushed.

## 2026-07-09 (same session) — D1 DONE + LIVED: NightScribe re-homed on KEEL; THE BOUNDARY HELD

The first real cell, built cross-language (C#/.NET 9 -> KEEL over protocol). **The 23 falsifier verdict: zero frozen-contract/golden edits were forced** — every KEEL-side change the cell demanded landed in legal layers (L2 adapter, L1 kernel-internal behavior, L5 app).
- **KEEL side** (`cb856b1`): whisper adapter gains `transcribe_segments` (`-oj`, ms offsets — the enrichment the minimal cut deferred); `keel-serve` gains **`/v1/audio/transcriptions`** (sidecar-local `path` request — a 2-hour WAV never crosses the wire; I3); whisper cli/model now keel.lock-driven (`servers.whisper.exe`, `substrate.audio.file`). **LIVED:** TTS WAV -> 2 ms-offset segments, correct text.
- **NightScribe side** (photo2deck `32c4f9a`, 101/101 tests): `KeelBackend : IVisionTextBackend` (chat+vision via `serve_openai`; `sovereign:true` on every call — the lab''s no-cloud invariant now ENFORCED by KEEL''s router; `grammar` = its JSON schemas) + `KeelTranscriber : ITranscriber` (ears via the new endpoint) — **both now the DEFAULTS** (`--vision-backend keel` / `--backend keel`; legacy `local`/`whisper` one flag away). The cell''s managed-llama-server subsystem (spawn/port/restart logic) is bypassed in keel mode. Every synthesis lands on KEEL''s Tape/audit and rides A7 recall automatically.
- **LIVED:** `nightscribe snap --ask "any text visible?"` -> screenshot -> KEEL -> sovereign->local Qwen vision under schema-constrained decode -> **rich schema-valid verdict, exit 0** (accurately read the operator''s screen).
- **Three genome bugs surfaced by the cell + fixed same-session** (`68b6f54`) — the falsifier earning its keep: (1) `kernel::lifecycle` launched llama-server **without `--jinja`** -> `chat_template_kwargs` (the thinking toggle) silently ignored; (2) **constrained decode + a thinking prefix are mutually exclusive** (grammar sampler breaks on a template-emitted `<think>`; llama 400) -> `local_llama` forces thinking OFF whenever a grammar rides the request; (3) **the engine stacked a second system message** above a caller-supplied one (jinja: system first + once) -> the memory preamble now **MERGES** into the caller''s system message — protocol-composition correctness for every OpenAI consumer, +2 kernel tests.
- **Boundary finding (recorded in ROADMAP):** for an over-protocol cell, afferent capture + the dHash/VAD gate stay CLIENT-side (capture lives where the devices are); the from-KEEL change-gate applies to embedded cells. Cell periphery kept exactly as the D1 map said: capture topology, attribution, timeline fusion, minutes schemas, eval harness, intent-memory db, GUI.
- **Gate: 159/6 green, clippy clean, seal `db4377b3` unmoved.** ROADMAP D1 -> `[x]`. NEXT: C1/C2 (embed model now on disk) · B1 · E2. This commit. Pushed.

## 2026-07-10 — C1/C2 recall benchmarks: golden-recall set DESIGNED + harness LANDED (design-review slice)

Supervised session (operator: "go ahead with C1/C2, propose the golden-recall set design"). Reconstituted via the protocol (HEAD `8511407` clean/pushed, gate re-confirmed 159/6 + clippy + seal before touching anything). One slice, per the NEXT_SESSION queue discipline: **design-review first — propose, then measure**; nothing decision-grade runs before ratification.
- **The structural anchor (drives the whole design):** the frozen `GOLDEN_RECALL` uplift case references the set BY NAME (`"set": "golden-recall"`) — so the labeled set is a **sibling artifact** (`tests/recall/golden-recall.json`), never an edit to the sealed `golden.json`; and the golden leaves `threshold` symbolic — so the **threshold values are ratified WITH the set** (they live in the set file).
- **Proposal:** `docs/proposals/golden-recall-set.md` — what C1/C2 decide + against what; set composition rationale; proposed thresholds (C1 recall@5 uplift >= 0.10 + p95 <= 1500 ms budget; C2 nDCG@10 uplift >= 0.05; base sanity floor 0.70); the 3-run measurement plan; statistical honesty (30 queries = smoke-scale, thresholds set above quantization noise, N stated in any decision); the operator list; non-goals (instruct-prefix knob, floor tuning, sqlite-vec stay closed).
- **DRAFT set:** 42 docs (35 turns + 7 episodes, incl. pure distractors) / 30 queries / 6 families — paraphrase (8, the C2 signal) · keyword_trap (7, the C1 signal: lexically-magnetic distractors vs low-overlap true answers) · entity (4, BLUEFIN-shape plants) · episodic (4, targets the coarse tier) · multi (4, graded ordering) · negative (3 -> top-1 cosine = relevance-floor calibration data; the live floor is `cos <= 0`). All content FICTIONAL (the repo is public); doc texts mirror the live Ring-4 shapes exactly (`FileMemory::summarize` / `Episode::text()`), so the bench measures the as-deployed text forms. `ratified:false` + null by/date until the operator flips it.
- **Machinery (all model-free-tested):** `svc::recall` gains the `Rerank` seam + `IdentityRerank` (the keel.lock `default: identity` made literal) + `rank_all` (scored ranking; `recall_top_k` now its id-view) + recall@k/nDCG@k/MRR + `RecallSet` loader with a **structure-only lint** (ids/kinds/families/grades/negatives — never content, so operator ratification edits cannot break the gate; the DRAFT file itself is lint-tested in CI) + `run_recall_bench` (embed corpus+queries -> cosine -> optional top-N rerank funnel -> per-family metrics + embed/rerank p50/p95; stub Embed/Rerank exercise it end-to-end, incl. an order-inverting adversarial reranker proving the funnel owns the final ranking). `keel-adapters::rerank::Reranker` (L2): llama-server `/v1/rerank`, dense scores indexed like the request, partial/out-of-range responses are honest errors; live test `#[ignore]`. **`keel recall-bench`** (L5): keel.lock-driven defaults (embed :8090, rerank :8091), `--rerank` enables the C1 leg, `--baseline` prints uplift (with set/version/k mismatch warnings), artifact -> `.keelstate/bench/` (verify-by-artifact); **DRAFT-stamped on every line until `ratified:true`**. `kernel::manifest` gains `substrate.rerank {id,file,port}` + `rerank_model_path()` (mirrors the embed organ; keel.lock gains `rerank.file/port` — config, not pins). Deliberately NOT built (S22): Ring-4 rerank wiring + lifecycle rerank-launch — only if C1 decides ON.
- **ISSUE-11 filed [operator-review]:** (1) ratify the set (edit freely, flip the flag); (2) provision `all-MiniLM-L6-v2` GGUF (~25-45 MB; already the keel.lock `embedding.fallback`) for the C2 leg; (3) contingent rerank-GGUF re-provision if the `--reranking` step-0 smoke refuses. Then the focused live run -> the two DECISIONS (WORKLOG + keel.lock flips).
- **Gate: 165 passed / 7 ignored** (+5 recall, +1 rerank-parse; +1 ignored live-rerank), clippy clean, **zero contract/golden edits, seal `db4377b3` unmoved.** This commit. Pushed.

## 2026-07-10 (same session) — the DRAFT smoke LIVED: harness proven on real organs; step-0 retired; the saturation finding

Post-commit, zero-cost, all local + bounded + file-redirected (ISSUE-8 pattern), everything DRAFT-stamped by the tool itself.
- **Stale-binary lesson (minor):** the first smoke ran an old `keel.exe` (dispatch absent -> "recall-bench" routed as a plain prompt; one junk scaffolding turn on the Tape, harmless/append-only). Rebuild blocked by the running `keel-serve` locking its sibling bin (os error 5) -> stopped it, rebuilt, **relaunched keel-serve with User-scope keys injected** (pid changed; :7070 healthy on the fresh build — the standing "restart serve after rebuilding keel" note, now lived).
- **Baseline leg (identity):** recall@5 0.975 / nDCG@10 0.869 / MRR 0.917 (n=27); embed p50/p95 18/30 ms. Family shape exactly as designed: traps found (recall 1.0) but mis-ordered (MRR 0.750) — the reranker''s headroom, where intended. Negative-control top-1 cosines **0.689/0.528/0.443** -> the live `cos<=0` floor would inject all three; a future floor sits ~0.7 on this embedder or stays relative (recorded as calibration data, no change made).
- **Step-0 rerank smoke PASSED (ISSUE-11 contingency retired):** `llama-server --reranking` on `qwen3-reranker-0.6b-q8_0.gguf` :8091 healthy in 6 s; `/v1/rerank` scored relevant 0.9975 vs distractor 1.3e-05 — the GGUF has the rank head; the response shape matches the adapter parser. Server killed after (box left as found: :8080/:8090/:7070 standing).
- **C1 rerank leg (DRAFT):** recall@5 1.000 / nDCG@10 0.930 / MRR 0.957 — **uplift +0.025 / +0.061 / +0.040**, improving EVERY family (traps MRR .750->.833, multi recall .833->1.0, paraphrase MRR .938->1.0); rerank p50/p95 **358/551 ms** (well under the 1500 ms budget). Artifacts: `.keelstate/bench/recall-qwen3-embedding-0.6b-q8-{identity,rerank}.json`.
- **The load-bearing finding: recall@5 SATURATES on the draft** (0.975 baseline -> +0.025 max headroom vs the golden-named C1 threshold >=0.10) — as drafted, C1 would mechanically decide OFF despite the reranker visibly improving ordering. **Remedy A recorded (proposal 7): harden to a v2 corpus before ratification** (grow ~2-3x with near-topic confusables; engineer identity recall@5 into ~0.6-0.8 using the per-query artifact data as the authoring feedback loop). Redefining C1''s input away from the frozen golden''s named measure was considered and rejected — fix the set, never bend a golden.
- Docs: proposal 7 addendum + ROADMAP C1/ISSUE-11 + STATE banner + NEXT_SESSION refreshed. Gate untouched since `73430c7` (165/7 + clippy stands; docs-only delta). This commit. Pushed.

## 2026-07-10 (same session, operator go: "go ahead with the v2 hardening pass") — golden-recall v2: hardened, measured, ready to ratify

The measurement-driven authoring loop the proposal promised, run to convergence. Everything DRAFT-stamped; zero contract/golden edits; all live runs bounded + file-redirected.
- **Harness first:** `QueryOutcome` gains `top_ids` (`#[serde(default)]`, the final top-10 ranking per query in every artifact) — the feedback loop that makes confusable-authoring surgical (which docs outrank the labeled ones, per query). +1 assert in the stub test. Rebuilt via `cargo build -p keel --bin keel` (the keel-serve sibling-bin lock avoided during the loop).
- **v2 authoring:** 42 -> 108 docs, 30 -> 41 queries (36 scored + 5 negatives). New confusable clusters around the hard queries (press-release/valve/bake for "software release fail" - derm/dental-hours/pet-dental for "dentist appointment" - dog-meds/pharmacy/vitamins for "my medication schedule" - recipe/mixer/yogurt/pizza for "sourdough starter" - vet-quote/cat-bill/copay for "vet visit cost" - steering/club/print-shop meetings for "neighborhood meeting" - etc). Two v1 queries re-specified for judge-clarity (q03 "my medication", q09 "software release"); NINE relevance maps extended where the new corpus made docs HONESTLY relevant (q06+n33, q16+n41, q21+n45, q22+n41, q27+n33, q10/q24+n35, q35+n37/n60, q39+n55) — label honesty over trap purity, every grade-0 defensible to a human judge. All fictional; live shapes preserved.
- **Three measure->reinforce iterations** (identity leg, real embed organ): 0.975 (v1) -> 0.886 -> 0.842 -> **0.786 — inside the 0.6-0.8 target window.** Family floor where designed: keyword_trap 0.611, episodic 0.694; paraphrase deliberately high (0.963). The `top_ids` data drove each round (three true docs sat at EXACTLY rank 5 after round two -> one clean magnet each tipped them out).
- **Draft rerank leg on v2** (:8091 launched + killed per ISSUE-8): **uplift +0.070 recall@5 / +0.111 nDCG@10 / +0.087 MRR** at rerank p50/p95 534/676 ms. Six queries recovered (q34 0->1 - the cross-encoder pulled the street-association episode back past the meeting-magnets; q06/q39/q35 to full; q10/q27 partial), ZERO regressions, four stay down for both models (q11/q14/q32/q33 — the traps fool the 0.6B cross-encoder too; honest hard cases, recorded not tuned away).
- **The C1 ratification call is now concrete, and deliberately NOT pre-made** (proposal 8): at the drafted `c1_recall_at_5_uplift: 0.10` the draft evidence says **OFF** (+0.070 falls short) despite large ordering gains; at 0.05 it says **ON** (+0.070 ~ 2.5 net queries at n=36, above the ~0.028 quantization noise). The threshold IS the operator''s policy call — exactly what "the set format is operator-authored ground truth" was for. C2 unchanged (blocked only on the MiniLM download).
- **Gate: 165 / 7 / 0 green** (the structural lint blessed v2), clippy clean, seal unmoved. keel-serve stopped for the gate relink + restarted healthy on the fresh build (keys injected). Docs: proposal 8 + ROADMAP C1/ISSUE-11 + STATE banner + NEXT_SESSION. This commit. Pushed.

## 2026-07-10 (same session, operator delegation: "go ahead and fetch the MiniLM GGUF, yes and proceed for all at your recommendation") — C1 + C2 DECIDED + LIVED; ISSUE-11 RESOLVED

The recommendation was stated BEFORE the ratified runs (pre-registration discipline): keep the drafted thresholds — moving a bar after seeing a number, in the direction that flips the outcome, is the motivated-reasoning trap the falsifier system exists to prevent. Everything below follows from that.
- **Ratification (delegated, recorded):** `tests/recall/golden-recall.json` -> `ratified: true`, `ratified_by` carries the delegation verbatim, thresholds unchanged (0.10 / 0.05 / 0.70 / 1500). The frozen `golden.json` + seal untouched throughout.
- **MiniLM provisioned byte-exact:** `second-state/All-MiniLM-L6-v2-Embedding-GGUF` f16 (45,949,216 B, content-length-verified) -> `C:\models\all-MiniLM-L6-v2-ggml-model-f16.gguf` (the honest floor: no quant loss, 45 MB, Apache-2.0).
- **Three decision-grade legs** on the ratified set (bounded, file-redirected; servers launched/killed per ISSUE-8; artifacts `.keelstate/bench/recall-{qwen3...-identity, qwen3...-rerank, all-minilm-l6-v2-identity}.json`): identity 0.786 r@5 (>= the 0.70 sanity floor) · rerank +0.070 r@5 / +0.111 nDCG@10 / p95 594 ms · MiniLM 0.906 r@5 / 0.840 nDCG@10 / p50 5 ms / negatives 0.29-0.49.
- **C1 DECIDED: OFF (identity stays).** +0.070 < 0.10. Rationale beyond the number: KEEL injects top-k memories WHOLESALE, so presence-in-top-k is the operative measure and ordering is secondary; the presence gain does not buy a permanent third managed server + ~0.5 s p95 on every assemble. The `Rerank` seam + adapter + bench stay built (a cell can wire them). Re-open: organic recall misses in lived use - a recall_k=1 pattern. keel.lock rerank block annotated.
- **C2 DECIDED: the FLOOR takes the default — a falsifier trip (expected direction INVERTED).** MiniLM beat Qwen3-0.6B-Q8 in EVERY family as-built (Qwen3 uplift over the floor = -0.122 nDCG@10 vs the +0.05 bar; episodic +0.25, traps +0.14; 3x faster; better negative separation). Caveats recorded honestly: the trap clusters were adversarially selected against QWEN3''s rankings (asymmetric — but the floor wins the UNTUNED families broadly, episodic alone decides it), and Qwen3 ran without its instruct-prefix query format (the as-built pipeline is what Ring-4 actually does). Re-open triggers: the instruct-prefix experiment - a symmetric-hardening set pass. This validates the falsifier system doing its exact job: "revise, don''t extend — blast radius one adapter."
- **The flip, executed + hardened + LIVED:** keel.lock embedding -> `all-minilm-l6-v2` / 384 / `pooling: mean`, fallback flipped to qwen3 (stays on disk); `EmbeddingCfg` gains config-driven `pooling` (manifest, default "last" = back-compat) wired through `resolve_embedder` -> lifecycle launch args; **write-side dim-guard** in `FileMemory` (fp_dim; a vector whose dim mismatches the fingerprint is dropped loudly — the stale-wrong-model-server sharp edge the flip exposed is closed end-to-end; reads were already safe via cosine->0 + the floor); **embed input head-capped** at 1500 chars (`embed_input`; MiniLM 512-token window; the stored/injected text stays full). +2 tests (dim-guard, cap). **LIVED by artifact:** stale Qwen3 embed server killed -> one `keel` call -> lifecycle cold-started MiniLM (`--pooling mean`, pid 28740) AND self-revived the dead main llama-server (pid 26240) -> fingerprint flip cleared the old sidecars -> **backfill rebuilt 0->30 vectors, all 384-dim** (`tape.vec.fp` = `all-minilm-l6-v2:384`) -> "Oslo", $0. The GOLDEN_RECALL rebuild-from-ledger mechanic lived under a real model change.
- **Docs:** proposal 9 decision record - ROADMAP C1 `[x]` / C2 `[x]` / ISSUE-11 RESOLVED - conformance-coverage.md recall row closed (only port gap left: D3 ToolHost) - STATE banner - NEXT_SESSION superseded. **Gate: 167 / 7 / 0 green, clippy clean, zero contract/golden edits, seal `db4377b3` unmoved.** keel-serve restarted on the fresh build. **NEXT: B1 (amplify clamped-OFF + ISSUE-4) - B3/ISSUE-5 - E2.** This commit. Pushed.
